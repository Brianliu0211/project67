# 📅 [2026-08-31] - 【模組二 / 模組五】條款爬蟲 Edge Function 實體對接、CORS 與動態時間修復

> **執行狀態**：🟢 已併入特徵分支 (`feature/beast-policy-crawler-sync`)，準備進行主線合併

## 🎯 實作成果摘要
修復條款爬蟲觀測中樞 (`DevConsoleScreen`) 之 `⚡ 檢查差異並增量更新` 按鈕連線，升級 Supabase Edge Function `crawl-insurance-products` [Version 5] 支援跨網域 CORS 預檢與 `insurance_helper_cron_secret` 內部排程密鑰，並將前端首頁/控制台載入時的「最後校對時間」由死字串改為動態查詢資料庫真相 `max(crawled_at)`。

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] [policy_crawler_service.dart](file:///c:/GitHub/project67/lib/services/policy_crawler_service.dart)**：
  - 徹底移除 `runIncrementalSync()` 的 Mock 模擬延遲，升級為真實發起 `_supabase.functions.invoke('crawl-insurance-products')` 請求，並帶入 `x-crawler-secret` 標頭。
  - 新增 `http.post` 備援請求機制，並精準處理網路例外與錯誤狀態碼（避免 200 悖論）。
  - 新增 `fetchLatestCrawledAt()` 方法，透過 Supabase 查詢 `policy_clauses` 資料表中 `crawled_at` 實體時間最大值。
- **[MODIFY] [dev_console_screen.dart](file:///c:/GitHub/project67/lib/screens/dev_console_screen.dart)**：
  - `_loadInitialData()` 初始化時呼叫 `fetchLatestCrawledAt()`，將「最後差量校對時間」動態呈現為真實資料庫時間。
  - `_handleIncrementalSync()` 依據 Edge Function 的真實 response `success` 欄位與 HTTP 狀態動態更新卡片 status badge (🟢 正常 / 🔴 異常) 與 Toast。
- **[MODIFY] [supabase/functions/crawl-insurance-products/index.ts](file:///c:/GitHub/project67/supabase/functions/crawl-insurance-products/index.ts)**：
  - 補齊全套 `corsHeaders` (`Access-Control-Allow-Origin: *`) 與 `OPTIONS` 預檢請求秒回 200 OK 邏輯。
  - 部署 Edge Function [Version 5] 至 Supabase 專案 `algufuoxkeizxwkofmmp`。
- **[DB / Schema] `profiles` 表權限更新**：
  - 將使用者 `huanghaolun58@gmail.com` 之 `role` 更新為 `dev`、`status` 更新為 `active`。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件/Skill 異動**：無。
- **密鑰/Edge Function 異動**：Edge Function `crawl-insurance-products` 升級 Version 5 部署生效。

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：`profiles` 權限變更經授權；Edge Function 保留 Service Role Key 寫入機制。
- [x] **UI/UX 規範**：符合霓虹/警告 Toast 標準與 RWD 卡片適配。
- [x] **工具包同步**：無新增 Package，套件庫一致。
- [x] **Null-Safety 防護**：`fetchLatestCrawledAt()` 與 `runIncrementalSync()` 具備 try-catch 降級安全保護。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：針對開發者回報條款爬蟲觀測中樞「筆數永遠 11,722 筆、按鈕顯示 HTTP 200 請求失敗」的問題進行深入診斷，成功釐清 Mock 模擬、CORS 預檢與假日期硬編碼三層因素，並完成實體對接與部署。
- **當前軟體狀態 (Current Software State)**：特徵分支 `feature/beast-policy-crawler-sync`，Web 本地測試伺服器跑在 `http://localhost:8080`，`flutter analyze` 通過 0 錯誤。
- **核心決策 (Key Architectural Decisions)**：Edge Function 強制補齊 CORS 標頭以符合 Flutter Web 跨網域存取規範；UI 時間一律動態查詢 DB 真相。
- **建議接棒下一步 (Recommended Next Steps)**：將特徵分支 `feature/beast-policy-crawler-sync` 合併回 `main` 主線，並 proceed 至進度表中其他排程或語意分析任務。
