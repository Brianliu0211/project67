# 📅 2026-08-14 - 【全模組雙向聯動與去假資料重構】

> **執行狀態**：🟢 已併入主線 (src) / 🟡 本地 feature 分支開發中

## 🎯 實作成果摘要
徹底拔除全專案（客戶管理、首頁行程、拓撲網格）中所有 `_mock` 假資料，並實現 Supabase 資料庫真實對接。首創「專案拜訪卡片」一鍵排入「行事曆」，並強化客戶刪除與轉介紹人設定的聯動防呆，確保模組間雙向資料邏輯正確無斷裂。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] `lib/screens/visit_projects_tab.dart`**：
  - 新增「排入行程 (Calendar)」按鈕，點擊後會喚起 `ScheduleEventDialog`，並傳遞 `initialTitle: '拜訪: ${project['title']}'` 與 `initialEventType: 'visit'`。
  - 確保專案拜訪與個人行事曆 (`schedule_events`) 達到真實資料的雙向流通。
- **[MODIFY] `lib/widgets/schedule_event_dialog.dart`**：
  - 擴充參數 `initialTitle` 與 `initialEventType`，支援從外部（如專案拜訪）直接帶入預設資料，且當 `id` 為空時正確觸發 `insert` 邏輯。
- **[MODIFY] `lib/screens/relationship_topology_tab.dart`**：
  - 將原先寫死的假資料 `DropdownMenuItem` 移除，改為透過 `_loadTopologyFromSupabase` 查詢 `customers` 表，取得 `_allCustomers` 以渲染真實客戶清單。
  - 當設定 `referral_source_id` 後，立即呼叫 Supabase `.update({'referral_source_id': selectedSourceId})`，徹底真實化拓撲關聯。
- **[MODIFY] `lib/screens/customer_management_tab.dart`**：
  - 全面清除 `_mockCustomers`。
  - 實作防呆警告：當點擊刪除客戶時，透過 `RichText` 明確提示這將連帶影響「專案拜訪」與「行程紀錄」。
- **[MODIFY] `lib/screens/home_screen.dart`**：
  - 移除 `_buildScheduleView` 內對假資料的依賴，全面改為依賴 `_fetchEventsForSelectedDate` 從 Supabase 的 `schedule_events` 表讀取真實行程資料。
- **[MODIFY] `lib/screens/admin_dashboard_tab.dart` & `profile_screen.dart`**：
  - 修復狀態讀取錯誤 (抓錯 `role` 欄位) 與點擊開通的實體更新。
  - 為 `dev` 工程師實作點擊狀態膠囊即可快速切換 `active/pending` 的真實聯動。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- 無環境或套件變更。所有連線皆基於現有 Supabase Schema 執行。

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：客戶與行程增刪改查皆已使用現有受到 RLS 保護的 policies。
- [x] **UI/UX 規範**：操作行為與防呆警告視窗 (`CustomToast` / `AlertDialog`) 符合 UI 標準。
- [x] **工具包同步**：無新增套件。
- [x] **Null-Safety 防護**：針對 `eventToEdit` 為 null 的情況，與客戶查詢為空的極端情況皆實作了例外處理與空值預設。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：專案人員要求檢查全專案的「雙向邏輯」，包括客戶管理、行程管理與人脈拓撲。核心精神為：若某功能被建立，它是否會合理地聯動到其他模組？所有缺乏實際資料庫支援的假功能皆必須被修復或替換。
- **當前軟體狀態 (Current Software State)**：目前已在本地 `feature/lobo-dev-roles-and-profile-auth` 分支完成所有邏輯修改，準備推上遠端。
- **核心決策 (Key Architectural Decisions)**：
  - 放棄單向的假清單，改以 `ScheduleEventDialog` 作為通用橋樑。`visit_projects` 不需要加入 date 欄位，而是直接轉發至 `schedule_events` 進行關聯儲存。
  - 轉介紹人 (Referral Source) 放棄樹狀圖的強制連線編輯，直接允許在拓撲節點跳出的選單中設定對應 `id`。
- **建議接棒下一步 (Recommended Next Steps)**：
  - 可以透過 GitHub Desktop 發布分支或切回 main 進行測試。
  - 下一步建議可以將 `home_screen.dart` 龐大的 `_buildScheduleView` UI 代碼重構進 `schedule_tab.dart` 中，讓主頁面更加精簡。
