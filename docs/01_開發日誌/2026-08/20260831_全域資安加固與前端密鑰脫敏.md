# 📅 [2026-08-31] - 【模組一：身分驗證、帳號連線與安全管理】全域資安加固與前端密鑰脫敏

> **執行狀態**：🟢 特徵分支驗證通過 (`feature/lobo-security-hardening`)

---

## 🎯 實作成果摘要
完成系統全域資安加固與金鑰脫敏工程：徹底移除前端明文 Demo 帳密與秘密後門、消除前端暴露之條款爬蟲 Cron Secret、強化 Supabase Edge Functions 存取權限驗證與 Bearer Token 查核、實作多使用者切換之離線快取隔離，並修復登入頁常用信箱網域快選之 UI 溢出問題，全套 27/27 項自動化測試全數綠燈通過。

---

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] [`lib/screens/login_screen.dart`](file:///c:/GitHub/project67/lib/screens/login_screen.dart)**：
  - 徹底移除前端暴露之硬編碼 Demo 帳密（`_performSecretDemoAgentLogin` 與盾牌 Logo 連擊秘密後門），確保商業發行版純淨度。
  - 修復「常用信箱網域快選」選單項目在窄版或多字元下的 `RenderFlex` 寬度溢出問題，使用 `Expanded` 與 `TextOverflow.ellipsis` 防溢出保護。
- **[MODIFY] [`lib/services/policy_crawler_service.dart`](file:///c:/GitHub/project67/lib/services/policy_crawler_service.dart)**：
  - 移除前端直接發送的明文 `x-crawler-secret: insurance_helper_cron_secret` 標頭。
  - 改以真實登入工作階段之 Access Token (`Bearer $token`) 呼叫 Supabase Edge Function，要求具備有效開發者權限。
- **[MODIFY] [`supabase/functions/crawl-insurance-products/index.ts`](file:///c:/GitHub/project67/supabase/functions/crawl-insurance-products/index.ts)**：
  - 消除預設寫死之 Secret 備援，伺服器排程僅接受環境變數 `CRAWLER_CRON_SECRET`。
  - 手動觸發模式嚴格要求 JWT 驗證，並查核 `profiles.role` 必須為 `admin` 或 `dev`，其餘請求一律回傳 401 Unauthorized。
- **[MODIFY] [`supabase/functions/voice-scheduler/index.ts`](file:///c:/GitHub/project67/supabase/functions/voice-scheduler/index.ts)**：
  - 強化授權防護：嚴格檢查 `Authorization: Bearer <token>` 是否存在且非空，並調用 `supabaseClient.auth.getUser(token)` 進行驗證，未授權立即 401 攔截。
- **[MODIFY] [`lib/screens/profile_screen.dart`](file:///c:/GitHub/project67/lib/screens/profile_screen.dart)**：
  - 將不合規的前端客戶資料客戶端直接刪除邏輯，替換為標準「受控後端註銷流程說明對話框」，防止前端誤判成功並消除資料殘留隱患。
- **[MODIFY] [`lib/main.dart`](file:///c:/GitHub/project67/lib/main.dart)**：
  - `OfflineDataStore` 實作 `ensureOwner(userId)` 與 `clear()` 機制，確保不同使用者在同一裝置切換時，舊使用者快取完全清空不洩漏。
  - 於 `AuthGateway` 補齊 `_AccessDeniedScreen`，若 Profile 狀態為 `deleted`、`suspended` 或空值，即刻阻斷存取。
- **[MODIFY] [`test/demo_golden_pipeline_test.dart`](file:///c:/GitHub/project67/test/demo_golden_pipeline_test.dart)**：
  - 將 Step 1.5 測試從過時的秘密盾牌連擊重構為「登入頁安全防護與常用信箱網域快選測試」，全套測試秒級通過。

---

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)

- **套件/Skill 異動**：無。
- **密鑰與安全異動**：
  - 前端代碼中已無任何明文硬編碼之密碼、Secret 或 Bypass 邏輯。
  - 遠端 Edge Functions `crawl-insurance-products` 需在 Supabase 後台配置 `CRAWLER_CRON_SECRET`（手動觸發則依賴開發者 JWT）。

---

## 🚦 Quality Gate 品質門檻稽核

- [x] **Supabase RLS 安全**：Edge Functions 補齊嚴格 Bearer Token 與 Dev/Admin 角色驗證；資料庫存取完全隔離。
- [x] **UI/UX 規範**：修復常用信箱網域快選選單之 RenderFlex 溢出問題；註銷彈窗符合 `main_spec.md` 規範。
- [x] **工具包同步**：無新增 Package/SDK。
- [x] **Null-Safety 防護**：所有 27/27 項測試（`customer_export_service_test`, `demo_golden_pipeline_test`, `widget_test`）全數通過（`00:03 +27: All tests passed!`）。

---

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)

- **會話脈絡 (Session Context)**：接手 Codex 因額度耗盡未完之安全加固工作，修復 E2E 自動化測試失敗與登入頁 RenderFlex 溢出問題，完成所有檔案之資安加固與驗證。
- **當前軟體狀態 (Current Software State)**：位於特徵分支 `feature/lobo-security-hardening`，所有測試通過，本地 Web 預覽正常。
- **核心決策 (Key Architectural Decisions)**：
  - 前端代碼嚴禁殘留任何明文帳密或繞過機制，Demo 示範一律使用標準登入或預設離線模式。
  - 條款爬蟲與語音排程 Edge Functions 一律透過 JWT 驗證呼叫者身分，徹底消滅前端暴露 Secret 隱患。
- **建議接棒下一步 (Recommended Next Steps)**：
  - 透過 GitHub Desktop 將 `feature/lobo-security-hardening` 推送至遠端並建立 PR 進行主線合併。
