# 開發日誌：人脈拓撲畫布流體滿版與權限隔離修復

**日期**：2026-08-19  
**開發者**：巨獸 (beast)  
**分支**：`feature/beast-fix-topology-canvas`  

---

## 1. 本次開發摘要

- **幾何佈局與流體滿版重構**：
  - 徹底廢除 `2000 x 1200` 硬編碼內層 Container，解決縮放時浮在中間的長方形暗色小箱子與邊界裁切問題。
  - 將畫布改為 `SizedBox` 流體自適應滿版，100% 無縫融合外層黑曜石面板。
  - 復原緊湊黃金座標中心 (`centerX = 600, centerY = 260`) 與縮緊環形半徑 (`ringRadius = 200`)，確保 10 個客戶節點 360 度完全呈現不被牆壁削邊。

- **Supabase 帳號降級與數據隔離**：
  - 配合開發者指示，執行 SQL 將 `haolun588@gmail.com (巨獸)` 的資料庫角色成功由 `dev` 降級為一般業務員 `agent`。
  - 修改 `relationship_topology_tab.dart` 中的資料載入邏輯，在一般業務員權限下強制加上 `profile_id = user.id` 過濾，實現完美的客戶個資隔離。

---

## 2. Quality Gate 品質稽核結果

- [x] **Supabase RLS 安全**：已於 `relationship_topology_tab.dart` 中注入 `profile_id` 嚴格過濾。
- [x] **UI 規範驗證**：符合 `main_spec.md` 滿版黑曜石與 Toast 規範。
- [x] **工具包同步**：本節未新增套件，`docs/工具包.md` 維持同步。
- [x] **Null-Safety 防護**：`_calculateClusterIslandTopology` 加入 `OfflineDataStore` 降級保底，避免空陣列抹除節點。

---

## [Handoff 狀態條言]

- **目前狀態 (Current State)**：
  - 已建立並自動切換至 `feature/beast-fix-topology-canvas` 特徵分支。
  - 本地 Web 預覽於 `http://localhost:8080` 正常運作，人脈拓撲畫布呈現乾淨流體滿版，且個人 6 位客戶資料隔離正常。
- **關鍵決策 (Key Decisions)**：
  - 一般業務員 (`agent`) 視角預設嚴格執行 `profile_id` 隔離；畫布尺寸與外層 Container 完全流體連動。
- **後續建議 (Next Steps)**：
  - 請於 GitHub Desktop 提交 Commit 並將 `feature/beast-fix-topology-canvas` 推送至遠端合併。
