# 📅 2026-08-31 - 【模組五：業務戰情中樞與排程】日程表三軌視圖與雙模組側邊欄重構

> **執行狀態**：🟢 已併入特徵分支 (`feature/beast-schedule-view-overhaul`)

## 🎯 實作成果摘要
- 重構「今日行程」板塊為 3 軌視圖切換器 (`月網格` / `日時間軸` / `議程清單`)，並實現響應式雙模組右側邊欄（月網格下為選定日期 24h 時間軸、日時間軸下為小月曆快速導航面板、議程清單下為滿版無右欄）。
- 修復首頁初始載入 Auth 異步恢復導致資料顯示全空白 Bug、24h 時間軸行程卡片標題擠壓遮擋 Bug、月網格跨日 7 天 Slot 行程連貫分配演算法，並移除議程清單過期歷史行程與打勾圓圈。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] `lib/screens/home_screen.dart`**：
  - **Auth Session 復原連動**：在 `initState` 加入 `onAuthStateChange` 監聽器與 `currentUser` 500ms 重試機制，解決初始載入為空的 Bug。
  - **月網格 7 天列位演算法**：引進 `occupiedSlots` 3 垂直槽位分配演算法，解決 7 天內超過 3 筆行程導致其他日期全空的 Bug，並移除右上角紫色數量圓圈。
  - **右側邊欄雙模組分流**：
    - `_buildDayDetailSidePanel`：月網格模式下顯示選定日期 24 小時滿版刻度網格 (00:00 - 23:00) 與同時間行程並排 (Side-by-Side) 佈局，Header 配置「+ 新增行程」按鈕。
    - `_buildMiniCalendarSidePanel`：日時間軸模式下顯示小月曆 Picker 與日期資訊卡片，點選即時切換左側 24h 日時間軸。
  - **日時間軸起點與卡片重構**：`_buildScheduleTimeline` 起始時間調整為 `00:00`，並添加今日實時紅點指示線；重構 `_buildCardSchedule` 排版，標題與標籤置於頂列，防止文字擠壓遮擋。
  - **議程清單滿版與歷史過濾**：`_buildAgendaListView` 於寬螢幕下 100% 滿版無右欄，依據 `DateTime.now()` 自動過濾已結束的歷史行程，並移除標題左側的打勾圓圈。
- **[MODIFY] `lib/l10n/app_zh.arb` & `app_en.arb` & `app_localizations.dart`**：
  - 補全 `calendar_view_agenda` 語系字串。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **無**。

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：本次未更動 SQL Schema，已有的 RLS 政策維持生效。
- [x] **UI/UX 規範**：符合 `main_spec.md` 之霓虹切換按鈕、彈窗與主題標準，無文字溢出或按鈕擠壓。
- [x] **工具包同步**：無新增 Package/SDK。
- [x] **Null-Safety 防護**：關鍵 DateFormat 與 List 轉譯具備 Exception 捕獲與防空值保護。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：完成了使用者針對「今日行程」板塊的連續數項重大 UI/UX 需求：月網格連貫 Banner 算法、24h 刻度並排網格、日時間軸小月曆導航面板、標題抗擠壓與議程清單滿版過濾。
- **當前軟體狀態 (Current Software State)**：程式碼已切換至標準特徵分支 `feature/beast-schedule-view-overhaul`，Web 伺服器運作於 `http://localhost:8080`。
- **核心決策 (Key Architectural Decisions)**：
  - 「月網格」右欄固定顯示該日 24h 時間軸細節。
  - 「日時間軸」右欄固定顯示小月曆，方便使用者點選直接切換日時間軸。
  - 「議程清單」滿版呈現場景，只專注於未來行程，過期行程不呈現。
- **建議接棒下一步 (Recommended Next Steps)**：
  - 透過 GitHub Desktop 將特徵分支 `feature/beast-schedule-view-overhaul` 發起 PR 併入 `main` 主線。
  - 若未來專案人員需要真正的 Google Calendar API 雙向同步，可安排申請 Google Calendar OAuth 權限並進行 API 串接。
