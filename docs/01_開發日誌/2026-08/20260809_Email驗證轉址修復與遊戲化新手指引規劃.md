# 📅 2026-08-09 - 【模組一 / 模組五】Email驗證轉址修復與遊戲化新手指引 RWD 規劃

> **執行狀態**：🟢 已併入主線 (lib/screens/login_screen.dart) & 🟡 規格與新手指引規劃階段

## 🎯 實作成果摘要
1. **修復 Email 註冊轉址漏洞**：在 `login_screen.dart` 的 `signUp` 中補全 `emailRedirectTo` 參數，自動帶入動態 Origin（`http://localhost:8080`），解決用戶在 Gmail 點擊驗證信後無法順利自動跳回應用程式的痛點。
2. **遊戲化解鎖成就與新手指引規劃**：擬定兩階段解鎖慶祝動畫（Confetti 噴彩帶 + 360 度轉圈徽章）與設定頁「章節式新手導覽與重播」架構。
3. **手機版 RWD 響應式跑板高優先標註**：特別標註目前在手機版行動裝置開啟時，部分 UI 元件溢出、卡片裁切與跑板的問題，納入最高優先級適配清單。

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] [`lib/screens/login_screen.dart`](file:///c:/GitHub/project67/lib/screens/login_screen.dart#L50-L69)**：
  - 引入 `package:flutter/foundation.dart` 的 `kIsWeb` 判斷。
  - 在 `supabase.auth.signUp()` 新增動態網址 `emailRedirectTo: redirectTo` 參數：
    ```dart
    String? redirectTo;
    if (kIsWeb) {
      final origin = Uri.base.origin;
      redirectTo = (origin.contains('localhost') && !origin.contains(':8080'))
          ? 'http://localhost:8080'
          : origin;
    } else {
      redirectTo = 'http://localhost:8080';
    }

    await supabase.auth.signUp(
      email: _emailController.text.trim(),
      password: _passwordController.text.trim(),
      emailRedirectTo: redirectTo,
      data: {
        'full_name': _nameController.text.trim(),
      },
    );
    ```

- **[SPEC] [`docs/00_公共規格/main_spec.md`](file:///c:/GitHub/project67/docs/00_公共規格/main_spec.md)**：
  - 補充遊戲化成就解鎖與新手指引模組（Confetti 彩帶 + 360 度轉圈徽章 + 章節式教學重播選單）。
  - 新增【手機端 RWD 響應式跑板優化與防溢出條款】。

- **[PROGRESS] [`docs/進度.md`](file:///c:/GitHub/project67/docs/進度.md)**：
  - 勾選完成 `[x] 登入與註冊 Email 驗證網址重定向修復 (emailRedirectTo: redirectTo)`。
  - 新增遊戲化新手指引與手機 RWD 跑板優化待辦卡片。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **無破壞性變更**。
- **Supabase 控制台設定**：Supabase Dashboard -> Authentication -> URL Configuration 中的 Site URL 需確認設為 `http://localhost:8080`，以利本地測試順暢。

## 🚦 團隊協作與驗證指南 (Verification Plan)

### 1. Email 註冊轉址驗證
- **測試指令**：使用 `run_local_web.bat` 或輸入「讓我看看」啟動 8080 本地伺服器。
- **驗證步驟**：
  1. 切換至「註冊帳號」，輸入全新 Email 與密碼送出。
  2. 開啟電腦端 Gmail 收信，點擊 `Confirm email address`。
  3. 確認 URL 參數包含 `redirect_to=http://localhost:8080` 並自動流暢跳回網頁首頁。

### 2. 手機版 RWD 跑板優化重點標註 (Mobile Responsive Optimization)
- **問題現狀**：當使用手機或縮小瀏覽器寬度（< 600px）檢視客戶卡片、月網格或彈窗時，部分元件會有溢出 (Yellow Overflow) 與文字裁切跑板。
- **下一步處置**：於後續版本引入 `LayoutBuilder` / `SingleChildScrollView` / `FittedBox` 全面盤點手機端適配。
