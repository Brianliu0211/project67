# 📅 2026-08-18 - 【模組一：基礎建設與安全防護】全域整修、跨平台解耦與測試綠燈完成

> **執行狀態**：🟢 準備透過 Standard PR 併入主線 (feature/lobo-platform-tests-refactor-f0-f2)

## 🎯 實作成果摘要
完成 P0–P2 (F0–F2) 全域接手整修：確認遠端 Supabase 生產環境 RLS 與安全 Trigger 全數生效，解耦前端 `dart:html` 平台依賴並建立跨平台 Helper，修復 CustomToast 生命週期 Timer 洩漏與 RenderFlex 排版溢出，達成全套 6 個單元/組件測試 100% 綠燈通過。

---

## 💻 技術變更明細 (Actual Technical Changes)

- **[NEW] [`lib/utils/platform_file_helper.dart`](file:///c:/GitHub/project67/lib/utils/platform_file_helper.dart)** / **[`platform_file_helper_stub.dart`](file:///c:/GitHub/project67/lib/utils/platform_file_helper_stub.dart)** / **[`platform_file_helper_web.dart`](file:///c:/GitHub/project67/lib/utils/platform_file_helper_web.dart)**：
  - 建立條件編譯跨平台檔案選取與下載抽象層，消除非 Web 環境下的 `dart:html` 編譯錯誤。
  ```dart
  import 'platform_file_helper_stub.dart'
      if (dart.library.html) 'platform_file_helper_web.dart';
  ```

- **[NEW] [`lib/services/voice_web_helper.dart`](file:///c:/GitHub/project67/lib/services/voice_web_helper.dart)** / **[`voice_web_helper_stub.dart`](file:///c:/GitHub/project67/lib/services/voice_web_helper_stub.dart)** / **[`voice_web_helper_web.dart`](file:///c:/GitHub/project67/lib/services/voice_web_helper_web.dart)**：
  - 封裝 Web 端 MediaRecorder 與 Web Speech API，解耦語音錄音服務對 `dart:html` 的直接依賴。

- **[MODIFY] [`lib/widgets/custom_toast.dart`](file:///c:/GitHub/project67/lib/widgets/custom_toast.dart)**：
  - 改用可取消的 `Timer? _dismissTimer` 取代 `Future.delayed`，在 `dispose()` 中主動釋放，根絕測試環境 Timer 洩漏。
  - 在 `CustomToast.show` 中使用 `WidgetsBinding.instance.addPostFrameCallback`，防止在 `initState()` 觸發 Toast 時引發 setState during build 例外。

- **[MODIFY] [`lib/screens/home_screen.dart`](file:///c:/GitHub/project67/lib/screens/home_screen.dart)**：
  - 側邊欄選單文字套用 `Expanded` 與 `TextOverflow.ellipsis`，防止文字溢出。
  - 時間軸標頭 `_buildTimelineHeader` 改用 `LayoutBuilder`（`maxWidth < 820`），解決內容寬度受限時的 RenderFlex 溢出問題。

- **[MODIFY] [`test/widget_test.dart`](file:///c:/GitHub/project67/test/widget_test.dart)**：
  - 補全離線跳過登入、HomeScreen 導航、客戶管理 Tab 載入與新增客戶對話框 UI 結構驗證測試。

---

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件異動**：無新增外部依賴，純 Dart/Flutter 內部架構解耦。
- **密鑰異動**：無。

---

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：遠端 `list_migrations` 確認 `20260818_database_hardening_final` 已套用，`pg_policies` 稽核為嚴格 owner-scoped 存取，`profiles` 觸發器已啟用。
- [x] **UI/UX 規範**：CustomToast 自動計算響應式寬度，移除所有 RenderFlex 溢出。
- [x] **工具包同步**：無需新增套件。
- [x] **Null-Safety 防護**：`flutter test` 6/6 測試案例 100% 綠燈通過。

---

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)

- **會話脈絡 (Session Context)**：
  - 嚴格依照最高接手契約 `docs/00_公共規格/AI接手與全域整修總規格.md` 與作戰書 `20260818_AI完整接手與全域整修作戰書.md` 推進 F0（遠端資料庫/Function部署真相確認）、F1（核心CRM閉環與降級架構）、F2（跨平台解耦與測試修復）。

- **當前軟體狀態 (Current Software State)**：
  - 當前特徵分支：`feature/lobo-platform-tests-refactor-f0-f2`
  - 測試狀態：`flutter test` 6/6 全部通過（耗時 2s）。
  - 編譯狀態：跨平台 `dart:html` 解耦完成，Web 與 VM/測試環境均能無痛編譯。

- **已確認之核心架構決策 (Key Architectural Decisions - 蘿蔔拍板)**：
  1. **主管（admin）角色權限**：**不允許**跨業務員檢視所有客戶資料（嚴格維持業務員個資隔離）。
  2. **開發者（dev）角色權限**：**允許**跨業務員檢視客戶資料（僅供系統稽核與技術排錯）。
  3. **標籤字典擴充**：**是**，未來開放一般業務員在前端自建「個人專屬標籤」。
  4. **專題 Demo 最小核心路徑**：**是**，鎖定「業務員登入 $\rightarrow$ 語音/手動建立客戶 $\rightarrow$ 語音智能排程 $\rightarrow$ 保單健檢與缺口計算」為現場必勝路徑。

- **建議接棒下一步 (Recommended Next Steps)**：
  1. **【F3 資料模型收斂】**：開立新分支 `feature/lobo-data-model-convergence-f3`，將 `customers.tags` 收斂至 `customer_tags` 關聯表，並清理 `schedule_events` 過渡欄位。
  2. **【F4 專題 Demo 黃金閉環打磨】**：以單一業務員身分驗證上述 4 步流程之 0 破綻流暢度。
