# 📅 [2026-09-21] - 【模組四：排程、日曆與戰情中樞】手機端手動新增行程按鈕現形

> **執行狀態**：🟢 特徵分支實裝 (`feature/peihan-mobile-add-event-btn`)

## 🎯 實作成果摘要
- 解決手機端（以及電腦瀏覽器寬度縮小至 < 768px 時）因側邊詳情面板隱藏導致使用者完全找不到「手動新增行程」按鈕的重大入口阻斷問題。在手機版頂部導航列 (AppBar) 右側補上「`+`」按鈕，點擊直達 `ScheduleEventDialog` 表單，徹底打通手機端測試大門。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] `lib/screens/home_screen.dart`**：
  - 在 `appBar: isWideScreen ? null : AppBar(...)` 的 `actions` 區域中，加入判斷式：當 `_activeMenu == '今日行程'` 時，渲染主題色 `IconButton`：
    ```dart
    if (_activeMenu == '今日行程')
      IconButton(
        icon: const Icon(Icons.add, size: 26),
        color: primaryColor,
        tooltip: context.l10n('event_add_title'),
        onPressed: () => _openAddEditEventDialog(),
      ),
    ```
  - 圖示採用主題色與 26px 尺寸，左鄰訊息通知中心小鈴鐺，具備 `context.l10n('event_add_title')` 多國語系 Tooltip。
  - 無論使用者切換至「月網格」、「日時間軸」或「議程清單」，頂部導航列均常駐可見，確保手動新增行程入口 100% 暢通。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件/Skill 異動**：無
- **密鑰異動**：無
- **資料庫/Schema 異動**：無（沿用既有 `schedule_events` 表與 `ScheduleService`）

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：未異動 SQL/Table，沿用既有 `schedule_events` 之使用者隔離 RLS 安全政策。
- [x] **UI/UX 規範**：符合 `main_spec.md` 之主題標準，使用 `primaryColor` 與多國語系 Tooltip，防抖動並支援觸控響應。
- [x] **工具包同步**：未新增 Package/SDK，`docs/工具包.md` 維持最新。
- [x] **Null-Safety 防護**：`_openAddEditEventDialog()` 具備完整空值校驗與異常攔截。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：沛沛進行 Bug 清單第一優先項目驗收，提出手機端（含電腦縮小視窗）因右側面板被隱藏而找不到手動新增行程按鈕之問題。經討論採納方案 A（手機端頂部導航列補上 `+` 按鈕）。
- **當前軟體狀態 (Current Software State)**：位於特徵分支 `feature/peihan-mobile-add-event-btn`，代碼已修改完畢，已產出互動式 HTML 模擬預覽與 Walkthrough 文件。
- **核心決策 (Key Architectural Decisions)**：新增行程按鈕綁定於手機版 `AppBar.actions` 區塊，限定於 `_activeMenu == '今日行程'` 時顯現，兼顧各分頁的介面簡潔性與日曆三視圖（月/日/議程）的通用存取性。
- **建議接棒下一步 (Recommended Next Steps)**：
  1. 使用 GitHub Desktop 將 `feature/peihan-mobile-add-event-btn` 分支推送至遠端，並發起 PR 合併至 `main`。
  2. 合併後由 GitHub Actions 自動編譯並部署至 `https://project67-red.vercel.app`。
  3. 沛沛使用手機打開線上網址進行實機點擊驗收，確認頂部 `+` 按鈕可正常彈出新增行程表單並儲存。
