# 📅 2026-07-15 - 【環境與部署】Vercel 部署舊版與 Flutter Web 編譯錯誤修復

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
- 解決 Vercel 重新連結 GitHub 後依然顯示舊版網頁的問題。
- 修正 Flutter SDK 升級導致的 Material 3 `CardTheme` 編譯型別錯誤，使網頁專案可順利在本地編譯並發布。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] `lib/main.dart`**：
  - 更新第一行註解以強制變更檔案，逼迫 Vercel 重新觸發部署。
  - 將 `cardTheme` 屬性的值從 `CardTheme` 修改為 `CardThemeData`，以符合新版 Flutter SDK 的 Material 3 主題型別規範。
  ```dart
  // 修正前
  cardTheme: const CardTheme(
    color: Color(0xFF161B22),
    elevation: 2,
  ),

  // 修正後
  cardTheme: const CardThemeData(
    color: Color(0xFF161B22),
    elevation: 2,
  ),
  ```

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **無**。未新增、刪除或升級 `pubspec.yaml` 中的相依套件。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗證**：
  在專案根目錄下執行以下指令進行編譯驗證：
  ```bash
  flutter build web --release
  ```
  應可順利通過編譯，產出檔案於 `build/web/` 下。
- **PM/夥伴預覽**：
  當 `build/web/` 的最新產物被 Push 到 GitHub 之後，Vercel 將自動更新部署網址。
