# 📅 2026-07-22 - 【模組一與二：個人設定與客戶管理優化】大頭貼選取、資料同步與檔名安全優化

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
- 實作了「客戶頭像」與「個人帳號頭像」的相機/相簿選取上傳功能（支援 Supabase Storage 與離線 Base64 雙軌儲存），解決了上傳中文檔名出錯的 `InvalidKey` 限制，並優化了 Web 端點擊頭像無反應的問題。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] [customer_management_tab.dart](file:///c:/GitHub/project67/lib/screens/customer_management_tab.dart)**：
  - 移除原有的「照片網址」輸入文字框，替換為全區可點擊的大頭貼 `InkWell` 與內嵌相機圖示。
  - 優化上傳邏輯，將使用者選取之檔案名稱以 `${timestamp}.${extension}` 重新安全命名，避免中文/空白字元導致 Supabase `InvalidKey` 錯誤：
    ```dart
    final extension = imageName != null && imageName.contains('.') ? imageName.split('.').last : 'jpg';
    final cleanExtension = RegExp(r'^[a-zA-Z0-9]+$').hasMatch(extension) ? extension : 'jpg';
    final fileName = '${user.id}/${DateTime.now().millisecondsSinceEpoch}.$cleanExtension';
    ```
- **[NEW] [profile_screen.dart](file:///c:/GitHub/project67/lib/screens/profile_screen.dart)**：
  - 建立個人帳號設定頁面，支援 RWD（響應式）排版，大螢幕雙欄/小螢幕單欄適配。
  - 連接 Supabase `profiles` 表，保存個人電話、公司、職稱、網站、通訊地址與簡介；離線狀態下自動持久化至 `SharedPreferences`。
  - 整合 `onProfileUpdated` 回呼，使得在儲存更新後側邊欄 header 內的頭像與姓名能即時刷新。
  - 嚴格使用自訂的 `CustomToast` 綠色 (Success)、黃色 (Warning) 與紅色 (Error) 通知系統。
- **[MODIFY] [home_screen.dart](file:///c:/GitHub/project67/lib/screens/home_screen.dart)**：
  - 側邊欄頂部 Header 區塊改為可點擊 (InkWell)，點擊後跳轉至「個人帳號」頁面。
  - 將導覽項目新增「個人帳號」，並優化 `_loadUserProfile` 支援大頭貼與姓名的雙軌加載。
- **[DB] [supabase_schema.sql](file:///c:/GitHub/project67/supabase_schema.sql)**：
  - 擴充 `profiles` 表欄位，新增名片相關的 7 個欄位。
  - 建立 `customer-photos` 與 `avatars` 儲存桶，並設定 RLS 安全防護策略（僅限本人操作自己 UID 資料夾內之物件，並開放公眾讀取）。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件異動**：新增 `image_picker: ^1.1.2`（已更新至 `工具包.md` 並通過本機工具包驗證）。
- **資料庫異動**：新增 `profiles` 表欄位與 Storage 儲存桶策略，已補入 `supabase_schema.sql` 中。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗飾**：
  - 執行 `puro flutter build web --debug` 確認網頁版打包編譯無誤。
  - 於 Supabase 控制台執行 `supabase_schema.sql` 的新增欄位與儲存桶 RLS 設定。
- **PM/夥伴預覽**：
  - 本地點選兩下執行 `run_local_web.bat`。
  - 進入「個人帳號」或「客戶管理」彈窗，直接點擊整個頭像區域選取照片，測試並儲存。
