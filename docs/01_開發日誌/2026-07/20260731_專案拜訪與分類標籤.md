# 📅 2026-07-31 - 【模組一：基礎建設與安全防護】專案拜訪與分類標籤

> **執行狀態**：🟢 已併入主線 (src) / 分支 `feature/beast-phase-1.5`

## 🎯 實作成果摘要
本階段完成了 Phase 1.5 客戶標籤的大分類結構正規化與 HSL 彩色質感渲染，並新增了一鍵將篩選客戶轉為「專案拜訪」純文字卡片清單的實作，支援完整的線上 Supabase 與離線 `SharedPreferences` 暫存儲存。

---

## 💻 技術變更明細 (Actual Technical Changes)

### 1. 資料庫設計與遷移
* **[MODIFY] `supabase_schema.sql`**：
  * 新增 `tag_categories`、`tags`、`customer_tags`、`visit_projects`、`visit_project_customers` 資料表，並開啟 RLS 權限管控。
  * 實作 `sync_customer_tags` 的 Trigger 函數，當直接變更 `customers.tags` 字串陣列時，自動將標籤同步分拆至正規化三層表中，維護舊功能相容性。

### 2. 標籤分類與樣式
* **[NEW] [tag_categorizer.dart](file:///c:/GitHub/project67/lib/services/tag_categorizer.dart)**：
  * 實作 `TagCategorizer.getStyle` 邏輯，根據標籤名正則匹配大分類（身分、險種、生活、健康、跟進），並回傳 Light/Dark Mode 對應的質感 HSL 背景與文字色。
  ```dart
  static TagStyle getStyle(String name, bool isDark) {
    // 依正則表達式進行身分、險種、生活興趣、健康體況等大分類匹配，返回軟配色
    ...
  }
  ```
* **[MODIFY] [customer_management_tab.dart](file:///c:/GitHub/project67/lib/screens/customer_management_tab.dart)**：
  * 修改客戶卡片（正背面）與放大彈出視窗的標籤 Chip 渲染，呼叫 `TagCategorizer.getStyle` 自動著色。

### 3. 專案拜訪卡片與導覽
* **[NEW] [visit_projects_tab.dart](file:///c:/GitHub/project67/lib/screens/visit_projects_tab.dart)**：
  * 實作專案拜訪頁面，展開後以純文字卡片清單呈現選中的客戶、電話、分類標籤、以及關鍵的備註內文（Remarks），點選姓名底線可彈出詳細放大檢視視窗。
* **[MODIFY] [home_screen.dart](file:///c:/GitHub/project67/lib/screens/home_screen.dart)**：
  * 側邊欄新增「專案拜訪」選單選項及圖示，並連結至 `VisitProjectsTab` 路由。

---

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)

* **【⚠️ 破壞性變更/環境異動】**：
  1. **資料庫異動**：線上資料庫需要手動貼上並執行 `supabase_schema.sql` 中的 Phase 1.5 升級 SQL 代碼。
  2. **環境升級**：本機 Puro 環境已將 Flutter SDK 強制升級至 `v3.44.8` 重新建立 Symlinks。
  3. **預覽腳本更新**：為了解決 Windows 上 Puro symlink path 導致 ddc 開發模式編譯失敗的已知問題，[run_local_web.bat](file:///c:/GitHub/project67/run_local_web.bat#L23) 已被配置為使用 `--release` 模式執行，以確保本機能夠 100% 成功預覽編譯。

---

## 🚦 團隊協作與驗證指南 (Verification Plan)

* **工程師驗證**：
  * 執行 `cmd.exe /c verify_toolpack.bat nopause` 驗證，規格應完全通過無警告。
  * 執行 `dart analyze` 檢查，結果應為 **0 編譯錯誤**。
* **專案夥伴預覽**：
  * 雙擊開啟 `run_local_web.bat`。
  * 瀏覽器開啟 **[http://localhost:8080](http://localhost:8080)** 後，可於首頁篩選客戶並點選「建立專案」；隨後點選側邊欄的「專案拜訪」，即可查看已儲存的專案與純文字客戶資訊卡片。
