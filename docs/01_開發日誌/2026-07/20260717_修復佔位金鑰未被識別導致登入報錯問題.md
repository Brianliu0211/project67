# 📅 2026-07-17 - 【部署自動化與安全防護】修復佔位金鑰未被識別導致登入報錯問題

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
- 修正主程式 `lib/main.dart` 中對預設佔位金鑰的過濾規則，新增對預設專案 URL (`algufuoxkeizxwkofmmp`) 與 Anon Key (`sb_publishable_...`) 的過濾，使其在未配置真實金鑰時能正確觸發「離線預覽模式」降級，避免本地開發或未配置金鑰的部署版本因嘗試連接虛擬服務而發生 decode JSON 報錯。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] `lib/main.dart`**：
  - 加強 `main()` 進入點處的環境變數過濾判斷：
    ```dart
    // lib/main.dart
    if (supabaseUrl == null || 
        supabaseKey == null || 
        supabaseUrl.isEmpty ||
        supabaseKey.isEmpty ||
        supabaseUrl.contains('your-project-id') ||
        supabaseUrl.contains('algufuoxkeizxwkofmmp') ||
        supabaseKey.startsWith('sb_publishable_')) {
      isOfflineMode = true;
      offlineReason = '檢測到預設金鑰占位字串';
    }
    ```

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- 無。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗證**：
  - 使用未修改的預設 `.env`（含有 `algufuoxkeizxwkofmmp` 與 `sb_publishable_`）啟動本地 Web 預覽，確認網頁頂部能正常顯示橘色「離線預覽模式」橫幅，且登入頁面出現「一鍵跳過登入」按鈕，點擊後能順利進入系統。
