# 待修正事項

以下為巨獸工作區整理的待修正與優化項目：

1. 📅 **首頁上方橫欄日期未即時更新**
   - **問題描述**：打開網站時，首頁上方橫欄（週曆）的日期不會即時更新為當前系統日期，會一直停留在 `7/8`（第一次開發時寫入的 Mock 日期）。
   - **待修復位置**：[home_screen.dart](file:///c:/GitHub/project67/lib/screens/home_screen.dart#L15)
   - **修正想法**：將 `_selectedDate` 的初始化宣告從寫死的 `DateTime(2026, 7, 8)` 改為動態載入系統當前時間 `DateTime.now()`。
