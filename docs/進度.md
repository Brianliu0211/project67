# 專案開發進度表

以下為 `insurance_helper` 專案目前的工作開發進度：

## 已完成工作 (Completed)
- [x] **Phase 0: 專案基礎建設初始化**
  - Flutter 專案建立與初始化
  - `.gitignore` 配置與 `.env` 本地變數宣告
  - `README.md` 說明文件配置
- [x] **Phase 0.5: 後端架構遷移**
  - 從原有的 Firebase 架構移轉至 Supabase (PostgreSQL)
  - 設計 `profiles`、`customers` 與 `reminders` 的資料表結構與關聯性
  - 實作 Row Level Security (RLS) 安全存取控制策略
  - 實作觸發器 (Trigger) 用於自動化建立 profile 與更新 `updated_at`
- [x] **Phase 0.6: 開發協作與危機自救體系建設**
  - 建立專案開發人員手冊，定義日常 Git 分支開發與收工操作流程
  - 規範 Line 記事本安全金鑰共享與 `.env` 配置範本
  - 建立統一的「緊急救援區」（由開發者工作區合併至公共規格）與 SOP 自救指南，保障專案持續性開發

- [x] **Phase 0.8: 專案管理規範與協作機制建立**
  - 建立開發行前檢查表 (`docs/00_公共規格/開發行前檢查表.md`)
  - 建立 Git 提交訊息規範 (`docs/00_公共規格/Git提交訊息規範.md`)
  - 建立金鑰範本 `.env.example`
  - 建立「收工」自動化指令（AI 自動寫日誌 + 更新進度 + 產出 Git 訊息）
  - 優化「開工」與「收工」自動化引導，健全「AI對話實驗室」沙盒規範
  - 建立本地 Web 測試版預覽啟動腳本與 AI 「讓我看看」自動化啟動指令
  - 預建專題報告暫存區 (`docs/04_專題報告/`)
  - 建立工具包自動化校對防呆機制與開發人員快捷指令手冊
- [x] **Phase 0.9: 部署管道與編譯環境修復**
  - 解決 Flutter SDK 升級帶來的 Material 3 `CardThemeData` 型別相容問題
  - 釐清並記錄 Vercel 託管 Flutter Web 靜態 `build/web` 的同步更新機制
- [x] **Phase 0.95: 部署自動化 (CI/CD)**
  - 設定 GitHub Actions 自動化編譯 Flutter Web 並部署至 Vercel (治本方案)
  - 自 Git 移除 `build/` 目錄的追蹤 (git rm -r --cached build)
  - [x] 支援 GitHub Secrets 於編譯期寫入 .env 以自動對接 Supabase
- [x] **Phase 1: 客戶基本資料庫與 UI 刻劃**
  - 串接 Supabase SDK（雙軌線上/離線資料儲存）
  - 刻劃響應式 (RWD) 客戶清單與卡片 UI (防溢出處理)
  - 實作包含搜尋、新增、編輯與刪除的完整 CRUD 對話表單功能
  - 實作自訂懸浮 Toast 通知系統，優化新增/編輯/刪除操作回饋
  - 實作 3D 翻轉客戶卡片動畫，並新增綽號與照片頭像欄位 (名片化設計)
  - [x] 修復 Supabase 線上資料表缺漏 nickname 與 avatar_url 欄位問題 (Hotfix)
- [x] **Phase 2: Supabase Auth 登入系統**
  - 業務員註冊與登入介面
  - 會話狀態保存 (Session persistence)

## 未完成工作 (Todo / In Progress)
- [ ] **Phase 1.5: 客戶模組與 UI 體驗優化** (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
  - [ ] 讓使用者自行上傳照片功能 (設定 Supabase Storage `customer-photos` / `avatars` Bucket 與 RLS 政策，引入 `image_picker`，上傳並回寫 `avatar_url`)
  - [ ] 客戶卡片新增放大/詳情檢視功能 (卡片雙面新增放大按鈕，設計 RWD 適配的彈窗/底欄展示姓名、電話、完整標籤與滾動備註)
- [ ] **Phase 2.5: 帳號管理與身分驗證優化** (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
  - [ ] 支援 Google 帳號第三方登入與連結 (Google Cloud 與 Supabase OAuth 憑證設定。**重要標註**：為配合未來 Gmail 讀取、Google 日曆同步及 Google Drive 匯出等功能，必須在登入/授權流程中規畫動態 Scope 請求機制，並確保能透過 Supabase Session 取得並保存 Provider Access Token 與 Refresh Token。於 `login_screen.dart` 新增按鈕並呼叫 `signInWithOAuth`)
  - [ ] 個人帳號管理頁面 (首頁側邊欄新增入口，對接 `profiles` 表，提供編輯姓名與上傳/更換大頭貼功能)
- [ ] **Phase 3: 語音錄音與文字轉錄**
  - [ ] 整合語音錄製功能並將錄音檔上傳至 Supabase Storage
  - [ ] 實作基本備註的儲存功能
- [ ] **Phase 4: Gemini API 語意分析核心**
  - [ ] 串接 Gemini API 進行語音轉錄文字的語意分析
  - [ ] 實作自動化客戶需求提取與重點摘要生成
  - [ ] 偵測並分析備註中具時效性的跟進時間
- [ ] **Phase 5: 排程與提醒推播 (含首頁日曆對接)**
  - [x] 首頁日期即時更新與週曆切換問題 (將 `_selectedDate` 改為動態取得 `DateTime.now()`，週曆兩側新增「上一週/下一週」按鈕) (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
  - [ ] 對接資料庫實作真正的日程日曆 (依選定日期查詢 `reminders`，關聯 `customers` 資料，時間軸由硬編碼改為動態渲染，新增/編輯行程表單) (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
  - [ ] 自動同步至系統日曆
  - [ ] 本地/系統通知與跟進提醒推播
- [ ] **Phase 6: 系統個性化與多國語系** (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
  - [ ] 系統設定頁面 (提供主題顏色切換、側邊欄折疊偏好、列表或卡片視圖切換，使用 `shared_preferences` 持久化)
  - [ ] 多語言選項支援 (引入 `flutter_localizations` 與 `intl`，建立 `app_zh.arb` 與 `app_en.arb`，支援繁中/英文切換與持久化)
- [ ] **Phase 7: 進階整合功能** (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
  - [ ] 電子名片生成與 Google 雲端硬碟匯出功能 (勾選資料動態生成卡片，支援轉圖/PDF 匯出或 vCard/.vcf 格式，整合 `google_sign_in` 與 `googleapis` 備份至 Google Drive)
- [ ] **系統架構與效能優化 (跨模組)** (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
  - [ ] 解決切換畫面或修改資料時整個頁面重整與重複讀取資料庫問題 (評估 `IndexedStack`、`AutomaticKeepAliveClientMixin` 進行 Widget 快取；改寫新增/修改/刪除邏輯為本地狀態同步更新 Optimistic Updates)


