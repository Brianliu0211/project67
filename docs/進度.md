# 專案開發進度表

本文件依據 [進度表維護規範](00_公共規格/進度表維護規範.md) 維護，以「五大核心模組」與「雙軌別名」紀錄 `insurance_helper` 專案之開發歷程與未來規劃。

---

## 📌 模組一：身分驗證、帳號連線與安全管理 (Auth, Accounts & Security)

### 🟢 Phase 0: 專案基礎建設與 Supabase 初始化 [已完成]
- [x] Flutter 專案建立與初始化
- [x] `.gitignore` 配置與 `.env` 本地變數宣告
- [x] `README.md` 說明文件配置
- [x] 從原有的 Firebase 架構移轉至 Supabase (PostgreSQL)
- [x] 設計 `profiles`、`customers` 與 `reminders` 的資料表結構與關聯性
- [x] 實作 Row Level Security (RLS) 安全存取控制策略與自動化 Trigger
- [x] 正式環境資安加固與資料庫真相統一 (套用單一責任 Migration 20260818 補齊 profiles 方案 A 團隊欄位、收斂 15 張表之 RLS 策略徹底消除匿名全開漏洞、修復 8 個資料庫函式 search_path、部署 Edge Functions 雙軌安全驗證與 GitHub Actions 排程)

### 🟢 Phase 0.6 & 0.8: 開發協作、危機自救、雙 AI 品質審查與專題發表簡報 [已完成 / 進行中]
- [x] 建立專案開發人員手冊與 SOP 緊急救援自救指南
- [x] 規範 Line 記事本安全金鑰共享與 `.env` 配置範本
- [x] 建立開發行前檢查表與 Git 提交訊息規範
- [x] 建立「開工」與「收工」自動化指令（含 Fast-Track 智慧分流）
- [x] 建立本地 Web 測試版預覽啟動腳本與 AI「讓我看看」自動啟動指令
- [x] 建立雙 AI 協作、AI接手契約與品質審查規範 (建立 `docs/00_公共規格/AI接手與全域整修總規格.md`，完成 F0-F2 遠端部署驗證、`dart:html` 跨平台解耦與 6/6 自動化測試全數綠燈通過)
- [ ] 專題發表簡報、報告講稿與展示素材產出 (置於 `docs/04_專題報告/`；包含 PPT 簡報、講稿、AI 宣傳影片腳本/連結、系統架構與資料庫 ERD 關聯圖)

### 🟢 Phase 0.9 & 0.95: 部署管道與 CI/CD 自動化 [已完成]
- [x] 解決 Flutter SDK 升級 Material 3 相容問題
- [x] 釐清 Vercel 託管 Flutter Web 靜態 `build/web` 的同步更新機制
- [x] 設定 GitHub Actions 自動化編譯 Flutter Web 並部署至 Vercel
- [x] 自 Git 移除 `build/` 目錄追蹤並支援 GitHub Secrets 自動對接 Supabase

### 🟢 Phase 2: Supabase Auth 登入系統 [已完成]
- [x] 業務員註冊與登入介面
- [x] 會話狀態保存 (Session persistence)
- [x] 登入與註冊 Email 驗證網址重定向修復 (`emailRedirectTo: redirectTo` 自動帶入當前 Origin，解決 Gmail 點擊無法跳回網頁之問題)
- [x] 註冊表單極簡化與 OTP 驗證過期重發防呆機制 (隱藏邀請碼與身分下拉選單，預設 active 授權，網址自動捕捉 `otp_expired` 警告與一鍵重發按鈕)
- [x] 註冊與登入防呆體驗修復 (未註冊帳號錯誤提示優化、密碼防窺眼睛按鈕、移除姓名自動填入、註冊成功彈窗新增直達信箱按鈕、修正註冊後直通開發者模式之角色異常)
- [x] 密碼重設獨立全螢幕頁面化與強制登出閉環 (`ResetPasswordScreen` 獨立視圖阻斷後台直接暴露，密碼設定成功後強制銷毀臨時 Session 登出並引導回登入頁)
- [x] Demo 專用業務員建檔與盾牌 5 連擊秘密真實登入 (建立 `niulai@helper.tw` Demo 一般業務員帳號與成都團隊，修正 Bcrypt 10 輪鹽值規範，登入頁 1.8 秒內連擊盾牌 5 次自動以真實身分向 Supabase 認證登入)
- [x] 全域資安加固與金鑰密鑰脫敏 (前端徹底移除明文 Demo 帳密與秘密連擊後門、移除前端明文爬蟲 Cron Secret、Edge Functions 補齊嚴格 Bearer Token 與 Dev/Admin 授權驗證、離線快取確保多使用者切換資料隔離、修復登入頁常用信箱網域快選溢出)

### 🟢 Phase 2.5: 帳號管理、第三方連線與驗證優化 [已完成 / 進行中] (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
- [x] 個人帳號管理頁面 (首頁側邊欄新增入口，對接 `profiles` 表，提供編輯姓名與上傳/更換大頭貼功能)
- [x] 登入介面忘記密碼 (Forgot Password) 重設流程 (新增 Email 重設密碼對話框，對接 Supabase Auth `resetPasswordForEmail`)
- [x] 📱 手機版 RWD 響應式跑板重構與多裝置適配 (全站 7 大畫面與頂層地圖 Modal 重構已完成，Web Release 與圖示防護測試零錯誤通過)
- [x] 設定頁第三方服務連線管理與帳號切換 (在設定與個人頁新增 Google 服務卡片；支援 Google OAuth 授權與帳號切換；`[Deprecated / 已廢棄]`: 原 LINE Login 第三方帳號連線已全面刪除清掃)
- [x] 支援 Google 帳號第三方登入與動態 Scope 授權 (Google Cloud 與 Supabase OAuth 憑證設定，支援 Session Provider Token 保存)
- [x] 開發者權限劃分與第三方連線追蹤 (於 Supabase `profiles` 表擴充 `role` 欄位支援 `admin` / `dev` / `agent` 角色權限、`is_google_connected` 與 `connected_providers` 欄位，完成 ProfileScreen 身分徽章與切換面板對接)
- [x] 個人帳號與數位名片 Master Studio 介面修整 (對齊真實頂級保險業務員實體名片視覺，新增「保險業務員登錄字號」欄位、保險經紀人/公司 Logo 區、專業證照標籤、一鍵點擊聯絡按鈕與背面服務據點 QR Code，徹底廢除 .vcf 檔案與怪異線條)
- [x] 帳號軟刪除 (Soft Delete) 與開發者實體清理 (Hard Delete) 雙層機制 (設定頁新增【刪除帳號】入口將 `profiles.status` 標記為 `deleted` 並自動登出；開發者控制台新增實體資料清理區塊，並完成 Supabase 資料庫欄位與 RLS 權限配置)

---

## 📌 模組二：客戶資產與名片庫 (Customers & CRM)

### 🟢 Phase 1: 客戶基本資料庫與 RWD UI 刻劃 [已完成]
- [x] 串接 Supabase SDK（雙軌線上/離線資料儲存）
- [x] 刻劃響應式 (RWD) 客戶清單與卡片 UI (防溢出處理)
- [x] 實作包含搜尋、新增、編輯與刪除的完整 CRUD 對話表單功能
- [x] 實作自訂懸浮 Toast 通知系統
- [x] 實作 3D 翻轉客戶卡片動畫，並新增綽號與照片頭像欄位 (名片化設計)
- [x] 修復 Supabase 線上資料表缺漏 nickname 與 avatar_url 欄位問題 (Hotfix)

### 🟢 Phase 1.5: 客戶模組與 UI 體驗優化 [已完成] (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
- [x] 讓使用者自行上傳照片功能 (Supabase Storage `customer-photos` / `avatars` Bucket 與 RLS，`image_picker` 回寫 `avatar_url`)
- [x] 客戶卡片新增放大/詳情檢視功能 (卡片雙面放大按鈕，RWD 彈窗/底欄展示姓名、電話、完整標籤與滾動備註)
- [x] 鍵盤 Enter 鍵提交輸入表單優化 (單行輸入框 `onSubmitted` 與 `Form` 提交)
- [x] 專案拜訪待辦清單 (篩選器 UI 與 `visit_projects`、`visit_project_customers` 表，記錄拜訪目的與可勾選 Checklist)
- [x] 分類標籤 UI 渲染 (客戶卡片與詳情彈窗依據標籤大分類進行 HSL 顏色區隔顯示)
- [x] 客戶全景沉浸式側邊抽屜 (Side Sheet 讀寫合一：電腦端 480px 側邊滑出，手機端自動切換底部彈窗；可直接於抽屜內讀寫電話、備註、標籤、自訂屬性並即時儲存)
- [x] 本地 0 成本保單健檢精算引擎 (5 大給付總額與自費缺口紅字警示，0 Token 消耗、完全 0 成本不課金秒算)
- [x] 跨公司商品 PK 條款陷阱比對 (借鑑 insure80：支援 2~4 款商品橫向對照健保 2-2-7 限制、雜費概括/列舉式、正副本收據等)
- [x] 待補齊名片/語音草稿收件匣 (客戶列表頂部展示金黃色草稿收件匣卡片，點擊 1 秒補齊轉正；極簡小鈴鐺通知待辦連動)
- [x] 資料庫標籤觸發器修復與 `tags` 唯一約束 (`sync_customer_tags` 支援空值校驗與 `ON CONFLICT` 安全容錯，解決 PostgreSQL 42P10 錯誤)
- [x] 開發者專屬「僅我建立 / 全系統客戶」視角切換器與建檔業務員真實關聯 (跨表查詢 `profiles` 即時呈現建檔業務員姓名、信箱與團隊，嚴格限 `dev` 角色可見)
- [x] 登入頁 Demo 快速通道開關與 5 連擊秘密手勢 (於開發者控制台一鍵切換正式商業登入 vs Demo 模式，登入頁盾牌連點 5 下秘密解鎖)
- [x] 專題 Demo 4 步黃金閉環 E2E 自動化測試 (`demo_golden_pipeline_test.dart` 秒級綠燈通過)
- [x] 動態自訂屬性 (JSONB) 與無損雙向往返 (Round-Trip) 匯入/匯出架構 (Supabase `customers.custom_attributes` 擴充、未映射欄位結構化存入獨立屬性、可逆前綴還原、Formula/XSS/vCard RFC 2426 折行安全防護、`test/customer_export_service_test.dart` 11/11 單元測試綠燈通過)
- [x] 分享/匯出快捷微動畫彈窗 (客戶全域工具列、多選浮動列「匯出選取 (N)」、全景抽屜全面接軌真實 `CustomerShareExportDialog`，支援 Excel `.xlsx` / UTF-8 BOM `.csv` / RFC 2426 `.vcf` / Print-Ready HTML 報表匯出)

---

## 📌 模組三：語音助理與 AI 語意分析 (Voice & Gemini AI)

### 🟢 Phase 3: 語音錄音與文字轉錄 [已完成]
- [x] 整合語音錄製與暫存功能，直接發送至 Edge Function 進行轉錄分析 (優化為不需永久上傳至 Storage)
- [x] 語音與手動輸入雙軌備註功能 (語音轉錄之文字框可隨時打字修改，亦支援直接手動輸入備註並儲存)
- [x] 建立 `visit_logs` 歷史日誌表與自動記錄 Trigger (定義拜訪類型與結果狀態，行程完成時自動寫入日誌)


### 🟢 Phase 3.5: 語音智慧行程排程 (Voice Scheduler) [已完成]
- [x] 新增「今日行程」麥克風 FAB 與霓虹呼吸光暈動畫 (深色模式 AnimationController + boxShadow)
- [x] 全螢幕磨砂玻璃錄音面板 (BackdropFilter sigma:15 + PulseCircle 脈衝波紋動畫)
- [x] Supabase Edge Function `voice-scheduler`：Groq Whisper STT + Llama 3.3 70B NLU 自適應輪替
- [x] 智慧客戶模糊匹配（自動去除稱謂尾碼）並寫入 `schedule_events`
- [x] JWT 驗證 Hotfix：Deno Stateless 環境中改用 `auth.getUser(token)`
- [x] 🟢🟡🔴 三燈防呆機制：後端回傳 `status` 與 `warning_messages[]`，前端自動分流綠燈關閉/黃燈手動調整/紅燈重錄

### 🟡 Phase 4: AI 語意分析核心進階功能 [待開發]
- [ ] 實作自動化客戶需求提取與重點摘要生成（從拜訪備註語音萃取）
- [ ] 偵測並分析備註中具時效性的跟進時間
- [ ] AI 語音局部更新與一鍵套用卡片 (調整 Prompt 限制 JSON null 輸出，前端新增 AI 建議卡片與一鍵套用邏輯)
- [ ] AI 標籤分類對齊與 Edge Function 限制 (新標籤提取時由 AI 自動分類並對齊指定大分類)

### 🟢 Phase 4.5: LINE 官方帳號與 Make.com AI 客服系統 [已完成]
- [x] LINE 官方帳號「音雪倫斯．艾希斯坦」與 🥕 AI 客服 Make.com 自動化流程與雙軌 Router 分流機制
- [x] Make.com 錯誤自動備援 (Error Handler) 與 Gmail 主動警報系統
- [x] LINE「正在打字中... 💬」Loading 動畫 API 串接與 4 格 Rich Menu 圖文選單佈署
- [x] AI Agent System Prompt 全面重構（同義詞模糊比對、語意獨立與解鎖原則）
- [x] Make.com 4 大系統錯誤診斷分類矩陣與 `Store incomplete executions` 雙重告警維運防護 SOP

---

## 📌 模組四：排程、日曆與戰情中樞 (Calendar & Operations)

### 🟢 Phase 5: 排程與提醒推播 (首頁日曆全面重構與雙視圖落地) [已完成 / 進行中]
- [x] 首頁日期即時更新與週曆切換問題 (動態取得 `DateTime.now()`，週曆兩側新增「上一週/下一週」按鈕)
- [x] 實作專屬行程資料表 `schedule_events` 與 CRUD 表單 (支援會議談判、客戶拜訪、提醒、個人行程與標籤分類)
- [x] 首頁版面兩欄式重構 (RWD：電腦版左月曆與摘要，右側視圖切換，手機版上下疊加)
- [x] 「日時間軸 | 月網格 | 議程清單」三軌視圖切換與 `SharedPreferences` 持久化 (網頁 F5 重新整理狀態保留)
- [x] 修復月網格 7 天列位連貫 Banner 算法、00:00 邊界與手勢 0ms 即時點擊
- [x] 實作日時間軸重疊行程並排演算法 (Side-by-Side) 與 00:00 起始滿版 24h 時間軸
- [x] 實作左右日曆雙向 100% 實時連動 (`ValueKey`) 與日時間軸小月曆導航面板 (`_buildMiniCalendarSidePanel`)
- [x] 實作議程清單 100% 滿版無右欄、自動過濾歷史過期行程 (`DateTime.now()`) 與標題抗壓迫卡片佈局
- [x] 實作並全站對齊 `CustomToast` 狀態彈窗 (青綠/黃/紅三色霓虹風提示)
- [x] 行程新增/編輯表單簡化 (刪除行程類型與關聯客戶下拉選單，簡化表單介面)
- [x] 語音智慧排程 Groq AI 連線修復與動態模型自動偵測 (修復 Web 本地預覽 audioBase64 空值導致 400 錯誤與 isOfflineMode 硬性阻斷，升級 Edge Function Version 12 實現 Groq 官方 API 在線 active 模型動態探測)
- [x] 雙欄平滑展開地點選擇器 (支援 OpenStreetMap / Nominatim 台灣在地搜尋、地圖大頭針點選與完整中文地址轉換)
- [x] 今日行程多站路線規劃與 Google Maps 免費導覽跳轉 (按時間順序勾選行程，起點自動鎖定目前位置，自動帶入多站 Waypoints 與終點導航)
- [x] 專案內現代化行程中樞全面落地 (重構 `ScheduleTab` 支援月曆網格、日時間軸、議程清單三視圖切換，建立 `ScheduleService` 響應式單例與離線快取守衛，整合智能關聯客戶下拉選單、拜訪重點備註、一鍵撥號與地圖導航，`test/schedule_service_test.dart` 4/4 單元測試與 27/27 全站測試綠燈通過)
- [x] 自動同步至 Google 日曆 (Phase 2：包含專屬次日曆自動維護、`google_event_id` 防重複寫入、`extendedProperties.private` 雙向屬性對齊與離線衝突防護)
- [ ] 週期性與循環行程支援 (RRULE; 支援每週/每月固定拜訪與保單繳費跟進提醒)
- [ ] 本地/系統通知與跟進提醒推播
- [ ] 行事曆客戶線上預約系統 (符合業務員空閒時段開放、行程自動遮蔽與 CRM 客戶資料連動掛載；兼顧保險經紀人角度支援保單健診/理賠諮詢/新保單規劃預約分類、團隊派單與合規告知聲明)

---

## 📌 模組五：系統個性化、情報與多國語系 (Preferences, i18n & Insights)

### 🟢 Phase 6: 系統個性化與多國語系 [已完成 / 進行中] (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
- [x] 系統設定頁面 (提供主題顏色切換、側邊欄折疊偏好、列表或卡片視圖切換，`shared_preferences` 與 Supabase User Metadata 雙軌持久化)
- [x] 多語言選項支援 (引入 `flutter_localizations` 與 `intl`，建立 `app_zh.arb` 與 `app_en.arb`，支援繁中/英文切換)
- [x] 側邊欄懸停自動展開功能 (與預設折疊開關合併為單一連動開關，簡化 UX 設定)
- [x] 設定頁「客戶卡片檢視模式」按鈕優化 (移至標題列右側並固定 260px 寬度防裁切)
- [x] i18n 補全：全站對話框 (`ScheduleEventDialog`, `VoiceSchedulerOverlay`, `CreateVisitProjectDialog`, `VoiceRecorderWidget`) 及月網格 `Month Grid` 標頭英文全數補全
- [x] 標籤管理器實作 (對接動態資料夾架構、顏色繼承與自訂色盤、同名防呆、一鍵合併標籤與全站客戶檔案同步)
- [x] 標籤大分類資料庫動態化與 16 色網格選擇器 (連動 Supabase `tag_categories` / `tags` 表與離線快取)
- [x] 全站標籤組件統一與 UI Design System 對齊 (抽離 `CategorizedTagAccordionSelector` 共享組件，徹底移除舊版純文字標籤框，並完成客戶管理、行事曆排程與個人帳號頁面 12px 圓角與按鈕階層視覺對齊)
- [ ] 全站開發語言與 i18n 語義徹底對齊 (將所有新增彈窗、對話框與 Toast 之 hardcoded 中文字串全數抽離至 `app_zh.arb` 與 `app_en.arb`)

### 🟢 Phase 6.5: 遊戲化新手導覽、解鎖成就與焦點遮罩教學 (Gamified Onboarding & Tour) [已完成]
- [x] 🎮 遊戲化解鎖成就與雙階段慶祝動畫 (採用 `confetti` 兩側紙屑彩帶噴發 + 360 度 3D 徽章卡片旋轉彈出；於『首次註冊登入成功』與『完成新手教學』雙階段觸發)
- [x] 首次登入自動觸發 Spotlight 聚光燈遮罩教學與迎賓彈窗 (實作 `SpotlightTourService` 與 `SpotlightTourOverlay` 全螢幕動態遮罩與氣泡 Tooltip)
- [x] 🎓 設定頁面「章節式新手導覽」與分區重播控制器 (於設定頁新增導覽專區，支援『播放完整新手導覽』與按 4 大章節分區選看/重播，並提供隨時跳過機制)

### 🟢 Phase 7: 進階整合功能與產業情報 [進行中] (對齊 [巨獸待辦](02_巨獸_工作區/想法存放區/待辦事項.md))
- [x] 每日保險新聞與權威產業動態 AI 自動整理系統
  - [x] 自動推送方案架構設計與技術選型 (整合 Google News 聚合 + 中央社、鉅亨網、工商時報、UDN 7 大 RSS 頻道；於 `docs/04_專題報告/` 歸檔專題規格報告)
  - [x] 於 Supabase 建立 `insurance_news_topics` 與 `insurance_news_articles` 資料表與 RLS 權限管理
  - [x] 實作 Edge Function `fetch-insurance-news` 串接 Groq API 進行 Google News 風格聚類與單段摘要
  - [x] 設定 GitHub Actions 自動化 Cron 每日排程打 API (一日兩更：台灣時間 06:00 晨報與 18:00 夕報自動觸發)
  - [x] 實作 App 前端「保險今日頭條」側邊欄與 RWD 新聞卡片 UI (Google News 聚類佈局、獨立媒體報導列表彈窗與單段 AI 重點摘要舒適排版彈窗)

- [x] 修復保險新聞手機與電腦版外連跳轉問題 (完全刪除前端死板假資料，並將 `url_launcher` 直跳媒體原生報導網址)
- [x] 智能保險新聞 24h 時效性過濾 (when:1d)、7 大媒體頻道擴充與夕報 18:00 追加模式 (Append Mode)
- [x] 智能保險新聞 Groq LLM 模型動態巡檢與自我修復機制 (Groq Dynamic Auto-Discovery；靜態模型廢棄時自動發起 Groq /v1/models 線上探測與彈性替代，實現 0 人工維護)
- [ ] 電子名片生成與 Google 雲端硬碟匯出功能 (勾選資料動態生成卡片，支援轉圖/PDF 匯出或 vCard/.vcf 格式，整合 `google_sign_in` 與 `googleapis` 備份至 Google Drive)
- [x] 人脈關係圖與轉介紹拓撲網絡 (黑曜石風格動態拓撲網格，支援 Supabase Real-time 對接、客戶卡片背面雙維度關係管理、子圖連通分量群島佈局、相鄰角度聚類、三軌道弧線避障演算法 +85px/0/-85px 徹底消滅標籤重疊、幾何質心全圖自適應置中與拖曳座標快取)
- [x] 數據戰情室與 SafeCheck 11,700+ 保單條款大一統庫存 (全量擴充全台 46 家公司與通路共 11,722 筆實體條款，包含 20 家壽險 + 26 家產險/通路，實作多關鍵字分詞交集搜尋、每頁 8 筆動態條件分頁與防跳動平滑切換，新客戶預設 0 假資料並對接 Supabase `customer_policies` 實體表)
- [x] 保險新聞管線動態自訂 RSS 來源管理 (建立 `news_rss_sources` 實體資料表，支援開發者即時新增、Ping 連線測試防呆與 Switch 開關控制)
- [ ] 工作照片管理功能 (獨立於大頭貼外的工作照片上傳、資料夾分類、掛載至客戶檔案下、及下載回手機功能)

---

## 📌 系統架構與效能優化 (跨模組)

- [x] 解決網頁重新整理時選單重置回首頁問題 (導入 SharedPreferences 持久化 activeMenu 狀態)
- [x] 實作 Shimmer 流光骨架屏載入與 Hover 縮放中心對齊 (解決載入閃爍與排版跳動)
- [x] 資料垃圾桶與 30 天自動永久刪除功能 (新增 `deleted_at` 軟刪除欄位，Supabase pg_cron 定時任務，實作垃圾桶 UI 復原與徹底刪除)
- [ ] 側邊欄選單重構與入口優化 (將「個人帳號 👤」提至頂層快捷區，並將「垃圾桶 🗑️」移至底部管理區)
- [ ] 手機版 RWD UI 擠壓防護與 Web App 滿版適配 (針對 `< 600px` 窄螢幕優化內距，並加入 `SafeArea` 防止 Web App 手機瀏覽器狀態列與導覽列遮擋)
- [ ] 劃分五大功能模組邊界並強制全站 Design System 一致性 (全站統一調用 `CustomToast` 狀態通知、`CategorizedTagAccordionSelector` 與 `BorderRadius.circular` 視覺規範)
- [ ] 解決切換畫面或修改資料時整個頁面重整與重複讀取資料庫問題 (評估 `IndexedStack`、`AutomaticKeepAliveClientMixin` 進行 Widget 快取；改寫新增/修改/刪除邏輯為本地狀態同步更新 Optimistic Updates)

---

## 📥 待評估與新想法暫存區 (Backlog / Incubator)

> 💡 *說明：本區塊用於暫存專案人員討論中剛萌芽、尚未確定具體模組或實作細節的新靈感。經討論確定採納時，將由 AI 剪切並向上搬移至上方對應的大模組中。*

- [ ] 電子信箱一鍵補全 @gmail.com 按鈕 (於登入頁、客戶 CRUD 表單與個人帳號編輯的 Email 輸入框旁新增快捷補全按鈕，簡化輸入步驟)
- [ ] SafeCheck 保單條款對照、數據戰情室與標籤大一統系統 (已對齊蘿蔔構想筆記 [20260810_SafeCheck借鏡與標籤大一統構想.md](01_蘿蔔_工作區/想法存放區/20260810_SafeCheck借鏡與標籤大一統構想.md))
  - [ ] Phase 1: SafeCheck 1.1萬+ 商品庫與 6 大白話檢查點 DB Schema (`safecheck_products`)
  - [ ] Phase 2: 標籤大一統 Engine 重構 (客群屬性、保障缺口、意願狀態三大分類)
  - [ ] Phase 3: 客戶詳情彈窗整合條款健診卡與 X vs Y 商品 PK 對決圖卡
  - [ ] Phase 4: 數據戰情室 (Data War Room) 全客群保障缺口熱力圖與 AI 熱勢提示
- [ ] 🎙️ 開發者全域語音原始文案觀測台 (評估於開發者控制台 `DevConsoleScreen` 建立全域語音轉錄日誌檢視機制，讓開發者可檢視使用者錄音之原始轉錄文案與 AI 萃取結果，以利校準語意模型偏差、持續完善 AI 在長文閱讀與意圖理解上的功能)



---

## 🗑️ 不考慮與歷史備忘區 (Archived / Rejected)

> 💡 *說明：本區塊用於紀錄經評估後決定「不採用/不考慮」的提案與短原因，供未來複查對照，保持正式待辦清單乾淨。*

- [ ] *(目前尚無經評估不採用的提案紀錄)*
