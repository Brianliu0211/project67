# 📅 [2026-08-10] - 【模組七：智能保險新聞】新聞頻道擴充、24h 時效性過濾與夕報累加模式

> **執行狀態**：🟢 開發完成，待發起 PR 併入主線 (`feature/beast-insurance-news-optimization`)

## 🎯 實作成果摘要
1. **剔除過時 category 分類**: 從 Edge Function 的 TypeScript 介面、Groq 系統 Prompt 與 SQL 寫入邏輯中完全剔除過時的 category 描述，節省 Token 並提升 AI 產出品質。
2. **擴充 3 大語意搜尋軌道與 7 大權威媒體頻道**:
   - 納入醫療險、實支實付理賠、長照險，以及國泰人壽、富邦人壽、南山人壽、新光人壽等龍頭巨頭動態。
   - 頻道涵蓋中央社、鉅亨網、工商時報、聯合新聞網、中時新聞網、ETtoday財經、鏡週刊理財。
3. **注入 `when:1d` 與 `pubDate` 雙重 24h 時間戳記攔截**:
   - Google News RSS 加入 `when:1d` 參數。
   - `parseRSSXml` 中精確比對 `pubDate` 實體新聞寫作發布時間，強制攔截丟棄過期舊聞，100% 解決舊文章出現在當日卡片的問題。
4. **實作夕報 18:00 追加模式 (Append Mode)**:
   - 改寫資料庫舊記錄清除邏輯，傍晚更新時讀取當日已有話題比對，將傍晚最新話題無縫累加入 Supabase。
5. **控管 Prompt Token 於 ~5,000 Tokens**:
   - 精選當日前 30 條 RSS 保險新聞傳給 AI，達到 Groq 6000 TPM 上限之 80% 容量，穩定產出 3~6 個話題與報導摘要。

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] `supabase/functions/fetch-insurance-news/index.ts`**:
  - `parseRSSXml`: 增加 `pubDate` 時間比對邏輯。
  - `clusterNewsWithGroq`: 移除 `category` 描述，調升傳入篇數至 30 篇。
  - `serve`: 更新 `rssUrls` 為 3 大語意軌道 + 7 大媒體頻道 (when:1d)，並將資料庫清空邏輯改寫為話題比對累加模式。

- **[MODIFY] `docs/02_巨獸_工作區/想法存放區/待辦事項.md`**:
  - 標記第 19 點「5000 Token 控管、when:1d 時效限縮與 18:00 夕報累加模式」為已完成 `[x]`。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **雲端與本機測試**:
  - 於 Supabase 清空當日舊記錄後，手動觸發 Edge Function 驗證成功產出當日鮮活新聞，且點擊文章直跳媒體官網。
