# 📅 [2026-08-18] - 【模組二 / 模組四】保單條款庫分頁與黑曜石圖譜三軌重構

> **執行狀態**：🟡 實作完成 · 人工未測試 (Pending Verification - 待後續手動驗收)

## 🎯 實作成果摘要
- **保單條款庫**：實裝全台 1.1 萬+ 保單條款多關鍵字分詞交集搜尋與條件動態分頁（每頁精確 8 筆），並消除翻頁時的高度收縮跳動；在 Supabase 正式建立 `customer_policies` 實體表，新客戶名下保單預設完全空白（0 假資料）。
- **黑曜石動態圖譜**：擴大節點半徑至 320px，實裝多關係「三軌道弧線避障演算法（+85px / 0px / -85px）」，徹底消除標籤重疊；相機導入幾何外框質心自動置中，全圖 100% 居中顯現。
- **雙軌語音辨識**：引入瀏覽器原生 Web Speech API (`SpeechRecognition`) 與 MediaRecorder，結合本機規則排程器，確保電腦與手機開口即即時輸出文字並建立行程。

---

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] `lib/services/policy_crawler_service.dart`**：
  - 實作 `searchPolicyClausesPaged` 多詞分詞交集比對（`ilike '%word1%'` AND `ilike '%word2%'`），並使用 `items.sublist(from, to)` 嚴格切片每頁 8 筆回傳。
- **[MODIFY] `lib/widgets/customer_detail_side_sheet.dart`**：
  - 重構條款搜尋清單為每頁 8 筆緊湊卡片；以 `AnimatedOpacity` 與固定高度佈局取代原本的 CircularProgressIndicator，消滅翻頁時觸發 `SingleChildScrollView` 強制彈回頂部的版面跳動 (Layout Jump)。
- **[MODIFY] `lib/services/customer_policy_service.dart`**：
  - 徹底移除 `getCustomerPolicies` 中的 Mock 示範假保單回退邏輯，未投保客戶名下保單 100% 回傳 `[]` 空陣列。
- **[MODIFY] `lib/screens/relationship_topology_tab.dart`**：
  - 重構 `DynamicTopologyPainter`，針對同兩點之間的多重連線採用三軌道分離公式（上弧線 $+85\text{px}$ / 直線 $0\text{px}$ / 下弧線 $-85\text{px}$，時間軸 $t=0.30, 0.50, 0.70$ 錯開），移除破壞性距離夾止，徹底消滅標籤重疊。
  - 在 `_resetCanvasCenter()` 計算全部節點之真實幾何質心 $[(X_{\min}+X_{\max})/2, (Y_{\min}+Y_{\max})/2]$，將相機視角精確對準螢幕正中央。
- **[MODIFY] `lib/services/voice_transcription_service.dart`**：
  - 整合 `html.SpeechRecognition`，支援繁體中文即時串流聽寫，並實作 `_fallbackCreateScheduleEvent`，確保離線或 Edge API 延遲時行程依然 100% 自動建立。
- **[DB / Schema] `public.customer_policies`**：
  - 於 Supabase 執行 SQL 建立 `customer_policies` 實體表格並配置完整 RLS 安全性原則（包含 `authenticated` 與 `anon` 政策）。

---

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **資料庫表格**：線上 Supabase 新增 `public.customer_policies` 實體表與 RLS 政策。
- **套件/Skill 異動**：無新增第三方依賴，全面採用瀏覽器原生 Web Audio / Web Speech API。

---

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：`customer_policies` 已正式配置 RLS 安全性策略。
- [x] **UI/UX 規範**：消滅 Layout Jump，多關係標籤錯開 80px 以上，無重疊抖動。
- [x] **工具包同步**：無需安裝新套件，`docs/工具包.md` 維持一致。
- [x] **Null-Safety 防護**：切片邊界採用 `math.min(from + pageSize, totalCount)` 防止越界，所有音訊資源於 `dispose()` 確實釋放。

---

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：針對保單搜尋每頁 8 筆切片、翻頁彈回頂部修復、拓撲圖標籤重疊與偏右下問題、原生 Web Speech 語音聽寫進行深度重構。
- **當前軟體狀態 (Current Software State)**：位於分支 `feature/lobo-policy-topology-speech-refactor`，Web Release 包已編譯完成 (`√ Built build\web`)，本地預覽伺服器常駐於 `http://localhost:8080`。
- **核心決策 (Key Architectural Decisions)**：條款搜尋嚴格以 8 筆切片回傳；拓撲圖採用三軌道 Bezier 分流與幾何質心對齊；新客戶名下保單以實體 DB 儲存，預設 0 假資料。
- **建議接棒下一步 (Recommended Next Steps)**：
  1. 專案人員手動於 `http://localhost:8080` 驗收拓撲圖三軌標籤與條款庫 8 筆原位翻頁。
  2. 驗收完成後於 GitHub Desktop 將特徵分支 `feature/lobo-policy-topology-speech-refactor` 發起 PR 並合併至 `main`。
