# 📅 2026-08-03 - 【模組五：系統個性化 & 模組二/四】標籤大分類動態化、全站標籤組件統一與 UI Design System 視覺對齊

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
完成標籤大分類之 Supabase 資料庫動態同步（支援 `tag_categories` / `tags` 表 CRUD 與離線快取），打造一格一格 16 色網格色碼選擇器，並抽取全站統一標籤手風琴元件 `CategorizedTagAccordionSelector`。同時徹底移除行事曆與客戶管理中殘留的傳統純文字標籤框，並建立全局 UI/UX 設計規範寫入 `main_spec.md`，完成全站輸入框 12px 圓角與按鈕視覺階層對齊。

## 💻 技術變更明細 (Actual Technical Changes)

- **[NEW] [`lib/widgets/categorized_tag_accordion_selector.dart`](file:///c:/GitHub/project67/lib/widgets/categorized_tag_accordion_selector.dart)**：
  - 抽取全站共享手風琴標籤選擇器，支援大分類單擊展開、Chips 卡片點選、`❌` 一鍵移除標籤與「+ 自訂色」/「⚙️ 管理大分類」對話框快捷呼叫。

- **[NEW] [`lib/widgets/color_palette_picker.dart`](file:///c:/GitHub/project67/lib/widgets/color_palette_picker.dart)**：
  - 打造 16 宮格現代調色盤組件（包含天藍、翡翠綠、琥珀黃、玫瑰紅、靛藍等預設質感色塊 + 自訂 HEX 色碼輸入框與實時預覽）。

- **[MODIFY] [`lib/services/tag_manager_service.dart`](file:///c:/GitHub/project67/lib/services/tag_manager_service.dart)**：
  - 新增 `getCategories`, `getTags`, `addCategory`, `editCategory`, `deleteCategory` 等 API 邏輯，並備有 `SharedPreferences` 離線降級保底備援機制。

- **[MODIFY] [`lib/screens/tag_manager_screen.dart`](file:///c:/GitHub/project67/lib/screens/tag_manager_screen.dart)**：
  - 支援大分類新增/編輯/刪除，將 `showAsDialog` 改為傳回 `Future<void>`，並將舊版 `ScaffoldMessenger` 洩漏提示改為彈窗獨態 `_bannerMessage` 浮動提示。

- **[MODIFY] [`lib/widgets/schedule_event_dialog.dart`](file:///c:/GitHub/project67/lib/widgets/schedule_event_dialog.dart)**：
  - 徹底刪除舊版 `自訂標籤/服務項目` 純文字框，導入 `CategorizedTagAccordionSelector`。
  - 將所有輸入框升級為 `BorderRadius.all(Radius.circular(12))` 圓角，確定按鈕對齊 `0xFF10B981` 綠色實心膠囊與灰色無邊框取消按鈕。

- **[MODIFY] [`lib/screens/customer_management_tab.dart`](file:///c:/GitHub/project67/lib/screens/customer_management_tab.dart)**：
  - 徹底刪除 `標籤 (逗號區隔)` 傳統單行純文字框，導入 `CategorizedTagAccordionSelector`。
  - 重構內部兩處 `buildInputDecoration` 舊樣式，升級為 12px 質感圓角 + `0xFFF8FAFC` 背景填色。

- **[MODIFY] [`lib/screens/profile_screen.dart`](file:///c:/GitHub/project67/lib/screens/profile_screen.dart)**：
  - 升級個人帳號頁面之 `buildInputDecoration` 與儲存變更按鈕，對齊 12px 圓角與主題高亮視覺。

- **[MODIFY] [`docs/00_公共規格/main_spec.md`](file:///c:/GitHub/project67/docs/00_%E5%85%AC%E5%85%B1%E8%A6%8F%E6%A0%BC/main_spec.md)**：
  - 新增 `## 📐 全局 UI/UX 視覺與對話框組件設計規範 (UI Design System)` 章節，定義圓角規格 (12px/20px)、輸入框 Icon 引導與雙階按鈕標準。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **無**。
- **資料庫 Table 需求**：如需線上同步大分類，請於 Supabase 執行建立 `public.tag_categories` 與 `public.tags` 之表格（若尚未建立將自動以 SharedPreferences 本地快取運行）。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **本機驗證**：在終端機執行 `run_local_web.bat` 或造訪 [http://localhost:8080](http://localhost:8080)。
- **體驗重點**：
  1. 開啟「客戶管理」與「行事曆行程」彈窗，確認舊版純文字標籤框已消失，完全替換為手風琴標籤選擇器。
  2. 開啟「個人帳號」、「客戶管理」與「新增行程」，確認輸入框圓角 (12px)、填色與實心藍/綠按鈕風格 100% 視覺統一。
