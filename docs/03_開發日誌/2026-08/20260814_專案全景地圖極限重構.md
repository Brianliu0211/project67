# 2026-08-14 專案全景地圖極限重構

## 1. 任務目標 (Objective)
- 對整個 Flutter 專案進行深度探勘與架構審查。
- 補全並重構 `docs/01_蘿蔔_工作區/想法存放區/專案全景地圖_草稿.md`，使其成為真正的單一真理來源 (SSOT)。
- 找出系統潛在的企業級邊界漏洞並定義未來的防護架構。

## 2. 實作重點 (Key Implementations)
- **多層次極限審查**：
  - **Pass 1-3**：盤點所有畫面 (包含隱藏的 `admin_dashboard`、`data_dashboard` 等) 的按鈕、表單與狀態邏輯，並畫出 Mermaid 動線圖。
  - **Pass 4**：審查資料庫，抓出單向人脈關聯漏洞、 Storage RLS 資安盲區，以及 ON DELETE CASCADE 孤兒紀錄風險。
  - **Pass 5**：執行企業級壓測審查，揪出 `tags` Array 引發的記憶體 OOM 危機、行程管理 `timezone` 錯亂風險，以及 Edge Function 的 API 帳單攻擊風險。
- **架構升級**：將上述所有「致命漏洞」寫入全景地圖的「第四區：未來藍圖區」，作為後續開發最高指導原則。

## 3. 面臨的挑戰與解決方案 (Challenges & Solutions)
- **挑戰**：原本地圖流於表面的 UI 清單，缺乏對資料庫連動、離線同步與大數據效能的考量。
- **解決方案**：透過多輪自我審查與深層爬梳，強制定義了諸如 LWW 離線衝突處理、無限分頁滾動 (Pagination)、以及本地 AES-256 儲存加密等基礎工程地基。

## 4. 下一步計畫 (Next Steps)
- 針對地圖中列出的「未來藍圖區」10 大地基進行實作。
- 首要任務可從重構 RLS 安全政策，或是補全 Onboarding Gate (消除幽靈帳號) 著手。

---

## [Handoff 狀態條言]
- **Session Context**: 完成了保險 CRM 專案的全面架構梳理與極限壓測審查，所有的實體程式碼現狀與未來升級藍圖皆已寫入 `專案全景地圖_草稿.md`。本次無程式碼更動，皆為架構文件。
- **Current State**: 處於 `main` 分支。架構文件已達 100% 完整解析，隨時可進行新功能開發。
- **Key Decisions**: 確認採用多對多標籤表、強制導入 Pagination、實作 LWW 離線機制與補全 Storage RLS 作為企業級標準。
- **Recommended Next Steps**: 開發者可以從全景地圖的 10 大地基中挑選一項（如：Auth Onboarding Gate）開始進行實作。
