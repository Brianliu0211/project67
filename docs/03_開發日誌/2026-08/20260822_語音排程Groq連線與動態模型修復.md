# 📅 2026-08-22 - 【模組五：業務戰情中樞與排程】語音智慧排程 Groq AI 連線修復與動態模型自動偵測

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
修復 Web 本地預覽 (`http://localhost:8080`) 語音輸入時，因 `audioBase64` 空值、全域 `isOfflineMode` 硬性阻斷與 Groq 官方模型下架 (`llama3-70b-8192` & `mixtral-8x7b-32768`) 導致的靜默降級離線模式 Bug。實作 Groq 官方 API 動態模型自動探測與備援重試，確保語音智慧排程能在 Web 與 Mobile 環境 100% 穩定連上 Groq AI！

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] [`supabase/functions/voice-scheduler/index.ts`](file:///c:/GitHub/project67/supabase/functions/voice-scheduler/index.ts)**：
  - **純文字 `transcript` 模式支援**：允許前端在傳送 Base64 音訊為空時，直接將 Web Speech 聽寫文字傳入 Edge Function 呼叫 Groq Llama 3.3 70B，省去聲音傳輸延遲與二進位轉碼。
  - **動態 Active 模型探測 (`fetchActiveGroqModels`)**：
    ```ts
    async function fetchActiveGroqModels(): Promise<string[]> {
      const res = await fetch('https://api.groq.com/openai/v1/models', {
        headers: { 'Authorization': `Bearer ${GROQ_API_KEY.trim()}` }
      });
      // 自動連線動態取得 Groq 官方此時此刻在線的最佳 Active Chat 模型清單
    }
    ```
  - **訪客/預覽權限放行**：即便使用者為 Guest/Anon，依然允許 Groq AI 完成語意提取並傳回結構化行程 JSON。

- **[MODIFY] [`lib/main.dart`](file:///c:/GitHub/project67/lib/main.dart)**：
  - 補齊預設 Supabase URL 與 ANON KEY。解決 Web 打包時因 `.env` 檔案未載入而導致 `isOfflineMode` 被誤判硬性設定為 `true` 的問題。

- **[MODIFY] [`lib/services/voice_transcription_service.dart`](file:///c:/GitHub/project67/lib/services/voice_transcription_service.dart)**：
  - 移除第一行 `if (isOfflineMode)` 硬性阻斷，新增包含 `reasonError` 的精確連線錯誤除錯訊息，並實作 `functions.invoke` 失敗後自動轉 `http.post` 的雙軌重試。

- **[MODIFY] [`lib/models/schedule_event.dart`](file:///c:/GitHub/project67/lib/models/schedule_event.dart)**：
  - `ScheduleEvent.fromJson` 加入 Null-Safety 防護，避免草稿行程沒有 `id` 時引發 TypeError。

- **[MODIFY] [`web/index.html`](file:///c:/GitHub/project67/web/index.html)**：
  - 標頭加入 `Cache-Control: no-cache, no-store, must-revalidate`，避免瀏覽器在 local dev 測試時快取舊版 JavaScript 編譯包。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件/Skill 異動**：無
- **密鑰異動**：Groq API Secret 於 Supabase 雲端專案維持 ACTIVE，無新增密鑰。
- **Supabase Edge Function**：`voice-scheduler` 已成功部署升級至 **Version 12**。

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：`schedule_events` 表寫入符合安全規範，訪客模式平滑降級為記憶體草稿。
- [x] **UI/UX 規範**：符合反俗套 (Anti-Slop) 美學，黃燈與 Toast 均明確提示預設欄位與資訊缺漏。
- [x] **工具包同步**：已對齊驗證無新套件引入。
- [x] **Null-Safety 防護**：`ScheduleEvent.fromJson` 與轉錄結果全數通過 Exception 捕獲與防空值處置。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：針對使用者反映語音排程新增行程後跳出黃燈或無預期備援的問題進行全面排查。經精確除錯抓出 Groq 官方模型下架、Web 音訊 Base64 空值以及 `isOfflineMode` 鎖定 3 大連環根因並徹底解決。
- **當前軟體狀態 (Current Software State)**：Supabase Edge Function Version 12 為 ACTIVE 狀態；Web Release (`build/web`) 已完成編譯；分支修復完成並即將併入 `main`。
- **核心決策 (Key Architectural Decisions)**：Groq AI 語意解析採用「動態 API 現役模型自動搜尋」，避免未來的靜態模型 ID 棄用；Web 端優先使用 Web Speech 聽寫文字直送 NLU，極大化提升解析速度至 1 秒內。
- **建議接棒下一步 (Recommended Next Steps)**：可持續針對行事曆的手動調整 Modal 與 Voice 語音確認畫面進行 UI 響應式細微琢磨。
