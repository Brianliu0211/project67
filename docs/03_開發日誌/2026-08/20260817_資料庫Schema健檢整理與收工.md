# 2026-08-17 開發日誌：外部 AI 資料庫 Schema 健檢整理與 Stage 1 零破壞加固落地

## 📋 任務概述
* **執行人員**：蘿蔔 (lobo) / Antigravity AI
* **本次主要工作**：
  1. 系統化彙整外部 AI 針對專案目前資料庫 Schema 提出的 15 項架構健檢、防呆與安全優化建議，產出標準化規格整理文件存放於蘿蔔想法區。
  2. 深度對照專案現有實體程式碼、全景地圖與 Supabase 結構，進行架構可行性與潛在衝擊性審查評估。
  3. **完成 Stage 1「零破壞性資料庫加固 (Zero-Breaking Hardening)」執行與線上驗證**：
     - 清洗與去重歷史重複文章資料。
     - 部署 6 項關鍵完整性約束 (CHECK / UNIQUE)。
     - 補齊 11 組高頻查詢與 Foreign Key B-Tree 索引。
     - 部署 `is_admin_or_dev()` 跨角色 RBAC 輔助函式。
     - 同步主架構真理來源 `supabase_schema.sql` 與建立 Migration 檔。

---

## 🛠️ 變更檔案清單
* **[NEW]** `supabase/migrations/20260817_database_hardening_stage1.sql`
  * 包含完整的資料清洗、約束、索引與 Trigger 升級語法。
* **[MODIFY]** `supabase_schema.sql`
  * 補充 Stage 1 資料庫加固區塊，保持與線上資料庫 100% 同步。
* **[NEW]** `docs/01_蘿蔔_工作區/想法存放區/20260817_資料庫Schema健檢與優化建議整理.md`
  * 完整收錄 15 大健檢項目、Top 10 優先級排序表、詳細 SQL 範例。
* **[MODIFY]** `docs/03_開發日誌/2026-08/20260817_資料庫Schema健檢整理與收工.md`
  * 紀錄本次整理歷程、加固執行結果與收工交接資訊。

---

## 🔍 Quality Gate 品質門檻稽核檢驗
* **[x] Supabase RLS 安全**：線上 15 張資料表全數啟用 RLS，並建立 `is_admin_or_dev()` 輔助權限過濾。
* **[x] UI 規範驗證**：零破壞性加固，前端 UI 完全不受干擾。
* **[x] 工具包同步**：無新增或變更外部 Dart/Flutter 相依套件。
* **[x] Null-Safety 防護**：所有約束與索引均通過線上 PostgreSQL 測試驗證。

---

## 🤝 [Handoff 狀態條言]
* **當前開發狀態**：**Stage 1 零破壞加固已 100% 成功套用至線上 Supabase 資料庫**，所有新約束與索引均已生效。
* **關鍵決策**：
  1. 提前清洗 `insurance_news_articles` 中的重複 URL，順利掛上唯一約束，免除日後 RSS 爬蟲髒資料問題。
  2. 將 `policy_clauses` 的唯一約束優化為 `(company_name, product_name)`，支援跨保險公司的同名商品儲存。
  3. 保留 `customers.tags` Array 供目前 Flutter UI 正常讀寫，待 Stage 2 時進行無痛資料搬移。
* **建議下一步**：
  1. 進行 Stage 2 準備：撰寫將 `customers.tags` 平滑遷移至 `customer_tags` 的資料遷移腳本。
