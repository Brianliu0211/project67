# 📅 [2026-08-31] - 【模組四：排程、日曆與戰情中樞】現代化行事曆中樞落地與Google串接預備

> **執行狀態**：🟢 特徵分支 (`feature/lobo-schedule-calendar`)

## 🎯 實作成果摘要
完成專案內現代化行程中樞全面重構與落地：建立 `ScheduleService` 響應式業務單例與離線快取防護，重構 `ScheduleTab` 支援月曆網格、日時間軸與議程清單三重視圖切換，升級 `ScheduleEventDialog` 支援客戶智能搜尋與帶入地址，完成專屬單元測試套件且全站 27/27 測試綠燈通過，並確立 Google 日曆雙向同步之次日曆與適配器架構（**明確標記：Google 日曆 API 實體串接為未實行，留待 Phase 2 接續推進**）。

---

## 💻 技術變更明細 (Actual Technical Changes)

- **[NEW] [`lib/services/schedule_service.dart`](file:///c:/GitHub/project67/lib/services/schedule_service.dart)**：
  - 實作單例模式管理全域行程生命週期，內建 `_currentUser` 守衛，離線與測試環境平滑降級至 `OfflineDataStore.scheduleEvents`。
  - 實作 `eventsRevision` 響應式通知器（`ValueNotifier<int>`），在行程 CRUD 時驅動全站日曆視圖毫秒級即時重繪。
- **[MODIFY] [`lib/models/schedule_event.dart`](file:///c:/GitHub/project67/lib/models/schedule_event.dart)**：
  - 擴充 `description` (詳細備註)、`google_event_id` (Google 事件 ID)、`google_calendar_id` (次日曆 ID)、`sync_status` (同步狀態)、`last_synced_at` (時間戳)。
  - 新增 `isGoogleSynced` 輔助計算屬性與 `copyWith` 不可變更新方法。
- **[MODIFY] [`lib/widgets/schedule_event_dialog.dart`](file:///c:/GitHub/project67/lib/widgets/schedule_event_dialog.dart)**：
  - 整合關聯客戶智能下拉選單（選定後自動帶入 `拜訪 [客戶姓名]` 標題與客戶通訊地址）。
  - 新增詳細備註/拜訪重點文字框，全量對接 `ScheduleService.instance` 集中式 CRUD。
- **[MODIFY] [`lib/screens/schedule_tab.dart`](file:///c:/GitHub/project67/lib/screens/schedule_tab.dart)**：
  - 重構為現代化行事曆中樞，支援 **📅 月曆網格 (Month Grid)**、**⏱️ 日時間軸 (Timeline)** 與 **📋 議程清單 (Agenda)** 三重視圖。
  - 依使用者回饋移除多維篩選列釋放視野高度，並將「＋ 新增行程」FAB 向左平移 76px 與右下角語音助理按鈕保持舒適並列間距。
  - 精緻行程卡片支援一鍵打勾完成（劃線刪除動效）、📞 一鍵通話、📍 Google Maps 導航跳轉。
- **[MODIFY] [`lib/screens/home_screen.dart`](file:///c:/GitHub/project67/lib/screens/home_screen.dart)**：
  - 今日行程導覽項目全面接軌新版 `ScheduleTab` 組件。
- **[MODIFY] [`lib/main.dart`](file:///c:/GitHub/project67/lib/main.dart)**：
  - `OfflineDataStore` 擴充 `scheduleEvents` 靜態陣列，提供離線與 Demo 模式完整資料持久快取。
- **[NEW] [`test/schedule_service_test.dart`](file:///c:/GitHub/project67/test/schedule_service_test.dart)**：
  - 建立 4 項專屬單元測試（JSON 序列化、UTC 轉換、copyWith 不可變性、CRUD 離線閉環、`eventsRevision` 響應式修訂通知）。

---

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **資料庫欄位擴充**：於 Supabase `schedule_events` 資料表新增 `description` (TEXT) 與 `sync_status` (TEXT DEFAULT 'local_only') 欄位。
- **套件/Skill 異動**：無新增外部依賴套件。
- **Google 日曆串接狀態**：**未實行 (Not Yet Implemented)**。目前已完成資料結構與適配器預備層，後續 Phase 2 需實作 OAuth Scopes 擴充與 Google Calendar API 串接。

---

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：`schedule_events` 原有 RLS 策略完備，擴充欄位皆受安全保護。
- [x] **UI/UX 規範**：符合 Anti-Slop 美學、深色/淺色主題自適應與響應式 RWD。
- [x] **工具包同步**：檢查確認未引入額外第三方套件。
- [x] **Null-Safety 防護**：`_currentUser` 守衛完備，全站 27/27 單元與整合測試全數綠燈通過。

---

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)

- **會話脈絡 (Session Context)**：
  本次會話聚焦於「專案內行事曆模組」與「Google 行事曆串接」之架構梳理與 Phase 1 開發。釐清了 Google 身分驗證 (Auth) 與 API 授權 (Calendar Scopes) 之差異、Google Calendar API 免費配額機制（每日 100 萬次免費 $0 成本）、以及次日曆公私隔離與 `extendedProperties.private` 欄位雙層適配方案。依照決策先完成 Phase 1 專案內行事曆之畫面與 CRUD。
- **當前軟體狀態 (Current Software State)**：
  - 目前位於特徵分支：`feature/lobo-schedule-calendar`。
  - 本地 Web 伺服器運作於：`http://localhost:8080`（`python -m http.server 8080 --directory build/web`）。
  - 自動化測試狀態：**27/27 Passed (100% 綠燈)**。
- **核心決策 (Key Architectural Decisions)**：
  1. **同步模式**：在 Google 日曆自動維護名為「**智慧保險小幫手**」的專屬次日曆，保障公私分離。
  2. **欄位適配**：內部專屬欄位（`customer_id`、`tag`、`is_completed` 等）將寫入 Google 事件之 `extendedProperties.private` 隱藏屬性，並在 `description` 生成格式化通話與導航筆記。
  3. **未實行標記**：**Google 日曆 API 實體串接目前為【未實行 / Phase 2 待辦】**，本次會話完成專案內全部畫面、資料模型與 CRUD 基礎建設。
- **建議接棒下一步 (Recommended Next Steps)**：
  1. 於 GitHub Desktop 提交本次變更並發起 PR 合併至 `main`。
  2. 開工 Phase 2：在 `login_screen.dart` 與 `settings_screen.dart` 擴充 Google Calendar Scopes，建立 `google_calendar_service.dart` 實現雙向同步引擎。
