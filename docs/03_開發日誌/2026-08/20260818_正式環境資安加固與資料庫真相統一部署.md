# 📅 [2026-08-18] - 【模組一：基礎建設與安全防護】正式環境資安加固與資料庫真相統一部署

> **執行狀態**：🟢 已完成遠端部署與特徵分支封裝 (`feature/lobo-database-hardening-and-security-patch`)

## 🎯 實作成果摘要
- 成功完成正式 Supabase 專案之「資安防護與資料庫真相統一」部署：套用單一責任 Migration 補齊 `profiles` 團隊欄位與防竄改 Trigger、收斂 15 張表之 RLS 政策消除所有匿名全開漏洞、部署 3 個 Edge Functions（`crawl-insurance-products` 雙軌安全驗證、`transcribe-voice` 與 `voice-scheduler` 401 攔截），並完成全套端到端多帳號與權限升權阻斷驗證。

## 💻 技術變更明細 (Actual Technical Changes)

- **[NEW] `supabase/migrations/20260818_database_hardening_final.sql`**：
  - 補齊 `profiles` 方案 A 欄位 (`team_id`, `team_name`, `is_google_connected`, `connected_providers`)。
  - 建立 `prevent_unprivileged_profile_security_changes()` Trigger，嚴格禁止一般業務員修改 `role`、`status`、`team_id` 與 `team_name`。
  - 清除 45 個舊有 Permissive Policies，全面重建嚴格 RLS 策略（`profiles` 刪除僅限 admin/dev、`customer_policies` 綁定客戶擁有者、`tag_categories` / `tags` / `news_rss_sources` / `insurance_news_*` / `policy_clauses` 改為全團隊唯讀且僅 admin/dev 可寫入）。
  - 修復 8 個資料庫函式之 `search_path` 並回收 PUBLIC 權限，消除 Database Linter 警告。

- **[MODIFY] `supabase/functions/crawl-insurance-products/index.ts`**：
  - 實作雙軌授權機制：優先驗證 `x-crawler-secret: $CRAWLER_CRON_SECRET`，次驗證 Admin/Dev User JWT。
  - 強制依賴 `SUPABASE_SERVICE_ROLE_KEY` 進行寫入，缺少金鑰立即 500 Fail-Fast，徹底移除匿名金鑰 fallback。
  - 未授權請求立即回傳 401，零資料庫查詢與零爬蟲呼叫。

- **[MODIFY] `supabase/functions/transcribe-voice/index.ts` & `voice-scheduler/index.ts`**：
  - 啟用 `verify_jwt: true`，部署至 Supabase Edge Runtime，阻絕未登入惡意呼叫。

- **[MODIFY] `.github/workflows/crawl_insurance_cron.yml`**：
  - 改以輕量 curl 攜帶 `x-crawler-secret` 標頭觸發 Edge Function，CI 完全不持有 Service Role Key。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **資料庫 RLS 規則全面收斂**：正式環境不再允許任何匿名或未授權寫入 `customer_policies`、`tags` 或 `policy_clauses`。
- **Edge Functions 部署**：3 個雲端函式已同步部署至遠端並生效。

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：15 張 public tables 啟用 RLS，全數建立嚴格安全策略。
- [x] **UI/UX 規範**：前端介面完全相容於後端方案 A 欄位定義。
- [x] **工具包同步**：未新增第三方相依套件。
- [x] **Null-Safety 防護**：Edge Functions 與 Trigger 全數具備防空值與 Exception 捕獲。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：完成正式 Supabase 環境資安與資料庫真相統一工作，由「只讀巡檢 $\rightarrow$ 預檢評估 $\rightarrow$ 修正計畫 $\rightarrow$ 單一正式 Migration 套用 $\rightarrow$ Edge Function 部署 $\rightarrow$ 多帳號端到端驗證」全流程閉環。
- **當前軟體狀態 (Current Software State)**：特徵分支 `feature/lobo-database-hardening-and-security-patch`，遠端資料庫與雲端函式已全部部署並驗證通過。
- **核心決策 (Key Architectural Decisions)**：
  1. `profiles` 採方案 A（保留 `team_id`, `team_name`, `is_google_connected`, `connected_providers`），由 `handle_new_user` 觸發器自動預設，禁止 client 端任意升權或竄改團隊。
  2. `tags` 與 `tag_categories` 採用全團隊共用字典庫模型，僅 admin/dev 可維護，一般業務員透過 `customers.tags` 與 `customer_tags` 關聯。
  3. `crawl-insurance-products` Edge Function 採用 `x-crawler-secret` Header 驗證與 Service Role Key 寫入。
- **建議接棒下一步 (Recommended Next Steps)**：
  1. 在 GitHub Desktop 中將 `feature/lobo-database-hardening-and-security-patch` 推送至遠端並建立 Pull Request 進行 Merge。
  2. 在 GitHub 倉庫的 Settings $\rightarrow$ Secrets 中配置 `CRAWLER_CRON_SECRET` 與 `SUPABASE_URL`，以啟用夜間自動化條款爬蟲排程。
