# 📅 [2026-08-19] - 【模組一：身分驗證、帳號連線與安全管理】Demo業務員牛來建檔與盾牌5連擊秘密登入

> **執行狀態**：🟢 特徵分支驗證通過 (`feature/lobo-demo-agent-quick-login`)

---

## ⚠️ 【重要：實機檢驗範圍與狀態聲明】（專案人員指定優先記載）

> 1. **本日實機檢驗範圍**：專案人員（蘿蔔）本日**針對【軌道 1：Demo 專用一般業務員「牛來 (成都團隊)」Supabase 真實建檔、Bcrypt 10 輪密碼修正，以及登入頁盾牌 Logo 5 連擊秘密真實登入（方式 B）】進行實機檢驗確認通過**。
> 2. **未檢驗項目說明**：其餘軌道（軌道 2 響應式佈局防溢出、軌道 4 語音排程、軌道 5 保單健檢精算）留待後續驗收。
> 3. **P5 延伸項目方針**：P5 延伸功能（外部 Google 行事曆、推播通知等）維持純架構藍圖保留，不擴張外部非核心代碼。

---

## 🎯 實作成果摘要

1. **Supabase 資料庫真實 Demo 一般業務員建檔 (`public.profiles` & `auth.users`)**：
   - 建立專用 Demo 帳號：`niulai@helper.tw`，密碼：`676767`。
   - 填入真實業務員 Profile：姓名「牛來」、團隊「成都」、電話「0967676767」、角色 `role: 'agent'`（真實一般業務員視角，受 RLS 嚴密隔離）。
   - 設定 `email_confirmed_at = now()`，完全不需外部真實 Gmail 驗證信。
2. **GoTrue 密碼驗證問題修復 (Bcrypt 10 輪鹽值規範)**：
   - 修正手動 SQL 寫入密碼時的 Bcrypt 鹽值輪數，採用標準 10 輪（`$2a$10$`）與完整空字串 Token 結構，徹底解決 `Database error querying schema` 錯誤。
3. **登入頁盾牌 Logo 5 連擊秘密真實登入（方式 B）**：
   - 登入頁平時為純淨商業版，無任何後門按鈕。
   - 藍綠色盾牌 Logo 🛡️ 連點 5 下（1.8 秒窗口）觸發真實 `signInWithPassword('niulai@helper.tw', '676767')`。
   - 攜帶完整真實 Session JWT 與 `currentUser.id` 自動轉跳首頁，並提示成功 Toast。
4. **自動化測試 10/10 全數綠燈通過**：
   - `test/demo_golden_pipeline_test.dart` 增加 **Step 1.5 盾牌 5 連擊秘密登入測試**，秒級綠燈通過。

---

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] `lib/screens/login_screen.dart`**：
  - 新增 `_performSecretDemoAgentLogin()` 處理真實身分驗證、Loading 狀態、Toast 與導航。
  - 在盾牌 Logo `GestureDetector` 掛載 `key: const ValueKey('shield_secret_logo')`、`behavior: HitTestBehavior.opaque` 與 1.8 秒 5 連擊偵測。
  - 連動 Dev Console 開關按鈕呼叫 `_performSecretDemoAgentLogin()`。
- **[MODIFY] `test/demo_golden_pipeline_test.dart`**：
  - 增加 Step 1.5 測試案例，覆蓋盾牌連點 5 下、離線與線上狀態轉換、HomeScreen 轉跳驗證。

---

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)

- **套件/Skill 異動**：無。
- **資料庫異動**：
  - 遠端 Supabase `auth.users` 與 `public.profiles` 新增 Demo 業務員 `niulai@helper.tw` (UUID: `45ebb774-cb4b-41e1-87a5-32c7f273b8ee`)。

---

## 🚦 Quality Gate 品質門檻稽核

- [x] **Supabase RLS 安全**：`niulai@helper.tw` 角色為 `agent`，完全受資料庫 RLS 隔離保護。
- [x] **UI/UX 規範**：登入頁純淨無後門，5 連擊具備時間防呆與 Toast 回饋。
- [x] **工具包同步**：無新增套件。
- [x] **Null-Safety 防護**：`flutter test` 10/10 測試全數綠燈通過（`00:03 +10: All tests passed!`）。

---

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)

- **實機檢驗現狀**：
  - ✅ **已實機驗收通過**：【軌道 1】Demo 業務員「牛來」建檔與盾牌 5 連擊秘密真實登入（方式 B）。
  - ✅ **前次已實機通過**：【軌道 3】客戶新增儲存、語音備註打標、Trigger 修復、Dev 視角切換器、建檔業務員真實關聯。
  - ⏳ **待後續驗收**：【軌道 2】RWD 響應式佈局、【軌道 4】語音排程、【軌道 5】保單健檢精算。
  - 🛑 **暫停項目**：P5 延伸功能（外部 Google 行事曆、推播通知等）維持架構保留。
- **當前特徵分支**：`feature/lobo-demo-agent-quick-login`
- **推薦下一步行動**：
  1. 在 GitHub Desktop 提交本次變更並建立 PR 併入 `main`。
  2. 下次開工時，按需驗收【軌道 4】語音排程或推進客戶匯出/分享功能。
