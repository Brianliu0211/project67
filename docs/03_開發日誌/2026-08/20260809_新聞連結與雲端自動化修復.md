# 📅 [2026-08-09] - 【模組七：智能保險新聞與 AI 趨勢分析】新聞連結跳轉與 Supabase 雲端 Edge Function 修復

> **執行狀態**：🟢 已併入主線分支 (`feature/beast-fix-news-link-redirect`)

## 🎯 實作成果摘要
1. **完全移除 100% 假資料**：徹底刪除前端 `_loadMockData` 死板範例數據，改為全自動對接 Supabase 資料庫。
2. **修復直跳新聞官網連結**：確保新聞頭條與文章卡片點擊時 100% 直跳媒體原生報導網址（非 Google News 搜尋頁面）。
3. **優化 Supabase Edge Function 全自動流程**：
   - 修正 404 失效媒體 RSS 頻道，升級為中央社 (CNA)、鉅亨網 (cnyes)、工商時報 (CTEE)、聯合新聞網 (UDN) 現網可用頻道。
   - 加入 Token 控管（精選 15 條權威新聞），完美符合 Groq 6000 TPM 免費額度限制。
   - 相容 LLM 輸出欄位名稱（`articles` / `items` / `news`）與 1-based 索引轉型，並修復 SQL RLS Policy 與 PostgREST Schema Cache 不一致問題。

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] `lib/screens/insurance_news_tab.dart`**：
  - 完全刪除 `_loadMockData()` 函式與假資料初始化。
  - 當 Supabase 資料庫無今日新聞時，平滑渲染排程空狀態提示：`"今日新聞定時更新中 (每日 06:00 與 18:00 自動定時抓取與 AI 聚類發布)"`。

- **[MODIFY] `supabase/functions/fetch-insurance-news/index.ts`**：
  - 控管輸入 Token：使用 `rssItems.slice(0, 15)` 將傳給 Groq AI 的 Token 數降至 ~1,200 TPM。
  - `max_tokens` 由 3000 調大至 4096，防止長文章 JSON 被截斷。
  - 相容多元 JSON 鍵名（`topic.articles || topic.news || topic.items`）並修復 `rss_index` 轉型。

- **[DB] `supabase/migrations/20260806_create_insurance_news.sql`**：
  - 補全 `daily_overview` 與 `daily_trend` 欄位。
  - 開放 `insurance_news_topics` 與 `insurance_news_articles` 之 `FOR ALL` RLS 存取權限。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **Edge Function 部署提醒**：需將更新後的 `supabase/functions/fetch-insurance-news/index.ts` 部署至 Supabase Cloud：
  `npx supabase functions deploy fetch-insurance-news --no-verify-jwt`

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **本機網頁測試**：
  在專案根目錄點擊執行 `run_local_web.bat` 或造訪 [http://localhost:8080](http://localhost:8080)，確認「今日新聞頭條」區塊完美渲染真實報導且點擊直跳新聞官網。
- **雲端排程測試**：
  前往 GitHub Actions 頁面 $\rightarrow$ 點選 `📰 Daily Insurance News Crawler & AI Summarizer` $\rightarrow$ 手動點擊 `Run workflow` 測試自動爬蟲與聚類。
