# 📅 2026-08-03 - 【模組五 & 模組六】側邊欄開關合併與全站 i18n 多國語系補全

> **執行狀態**：🟢 已併入開發分支 (`feature/beast-settings-i18n-opt`)

## 🎯 實作成果摘要
1. **側邊欄設定開關合併**：將「預設自動折疊邊欄」與「懸停自動展開邊欄」合併為單一開關，啟用折疊時自動啟用懸停展開，簡化 UX 設定流程。
2. ** SegmentedButton 寬度與單列佈局修復**：將客戶卡片預設檢視模式按鈕移至標題列右側，並固定 260px 寬度防裁切。
3. **全站 i18n 英文翻譯補全**：補全 `ScheduleEventDialog`、`VoiceSchedulerOverlay`、`CreateVisitProjectDialog`、`VoiceRecorderWidget` 及月網格 `Month Grid` 星期標頭 (`Sun`, `Mon`...) 等全站隱藏對話框與元件之英文翻譯。

## 💻 技術變更明細 (Actual Technical Changes)

- **[MODIFY] [`lib/services/app_settings.dart`](file:///c:/GitHub/project67/lib/services/app_settings.dart)**：
  - 在 `setSidebarCollapsedByDefault()` 中連動設定 `_isSidebarHoverExpandEnabled`。
    ```dart
    Future<void> setSidebarCollapsedByDefault(bool collapsed) async {
      if (_isSidebarCollapsedByDefault == collapsed) return;
      _isSidebarCollapsedByDefault = collapsed;
      _isSidebarHoverExpandEnabled = collapsed; // 懸停展開與折疊偏好連動
      notifyListeners();
      await _saveAndSync();
    }
    ```

- **[MODIFY] [`lib/screens/settings_screen.dart`](file:///c:/GitHub/project67/lib/screens/settings_screen.dart)**：
  - 移除獨立的懸停展開 `SwitchListTile`。
  - 將「客戶卡片預設檢視模式」`SegmentedButton` 移動至標題列右側 `Row` 中，並包裹 `SizedBox(width: 260)` 確保文字不被擠壓裁切。

- **[MODIFY] [`lib/screens/home_screen.dart`](file:///c:/GitHub/project67/lib/screens/home_screen.dart)**：
  - 日曆導覽列、`+ Add Event` 按鈕、`Today's Summary` 卡片及月網格視圖之星期標頭 (`Sun`~`Sat`) 全數連動 `AppSettings.instance.language` 動態翻譯。

- **[MODIFY] [`lib/widgets/schedule_event_dialog.dart`](file:///c:/GitHub/project67/lib/widgets/schedule_event_dialog.dart)**：
  - 引入 `app_localizations.dart`，將 `Add Event` / `Edit Event` 標題、`Start Date` / `End Date` 及按鈕英文化。

- **[MODIFY] [`lib/widgets/voice_scheduler_overlay.dart`](file:///c:/GitHub/project67/lib/widgets/voice_scheduler_overlay.dart)**：
  - 補全全螢幕語音面板之控制按鈕（`Cancel` / `Done` / `Discard Event` / `Manual Adjust` / `Re-record` / `Close`）。

- **[MODIFY] [`lib/screens/customer_management_tab.dart`](file:///c:/GitHub/project67/lib/screens/customer_management_tab.dart)** & **[`lib/widgets/voice_recorder_widget.dart`](file:///c:/GitHub/project67/lib/widgets/voice_recorder_widget.dart)**：
  - 補全 `Create Visit Project` 對話框及客戶備註錄音按鈕文字之英文化。

- **[MODIFY] [`lib/l10n/app_zh.arb`](file:///c:/GitHub/project67/lib/l10n/app_zh.arb)** & **[`lib/l10n/app_en.arb`](file:///c:/GitHub/project67/lib/l10n/app_en.arb)** & **[`lib/services/app_localizations.dart`](file:///c:/GitHub/project67/lib/services/app_localizations.dart)**：
  - 新增近 20 個全新 i18n 鍵值。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **無**。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗證**：在 terminal 執行 `puro flutter build web --release` 確認無語法與編譯錯誤。
- **PM/夥伴預覽**：開啟 `http://localhost:8080`，前往「系統設定」切換顯示語言為 `en_US`，即可驗證全站介面與對話框之英文對齊效果。
