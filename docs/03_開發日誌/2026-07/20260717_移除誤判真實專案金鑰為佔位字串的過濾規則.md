# 📅 2026-07-17 - 【部署自動化與安全防護】移除誤判真實專案金鑰為佔位字串的過濾規則

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
- 修正 `lib/main.dart` 中的金鑰過濾邏輯，將誤判為佔位字串的真實專案 ID 移除，確保系統能正常以「線上模式」連接 `algufuoxkeizxwkofmmp` 專案。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] `lib/main.dart`**：
  - 移除了對 `algufuoxkeizxwkofmmp` 專案 ID 的過濾判斷，恢復預設的佔位字串檢查邏輯：
    ```dart
    // lib/main.dart
    if (supabaseUrl == null || 
        supabaseKey == null || 
        supabaseUrl.isEmpty ||
        supabaseKey.isEmpty ||
        supabaseUrl.contains('your-project-id')) {
      isOfflineMode = true;
      offlineReason = '檢測到預設金鑰占位字串';
    }
    ```

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- 無。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗證**：
  - 確保程式能以 `https://algufuoxkeizxwkofmmp.supabase.co` 啟動，不被強制阻斷。
