# 📅 2026-07-19 - 【Phase 1.5】客戶卡片放大與詳情檢視

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
- 於客戶管理卡片正反面右上角新增「放大」按鈕，點擊後會以 Dialog 彈出響應式適配的客戶詳細資訊視窗，並支援一鍵複製電話/電子郵件至剪貼簿，免去業務員手動輸入的繁瑣過程。

## 💻 技術變更明細 (Actual Technical Changes)
- **[MODIFY] `lib/screens/customer_management_tab.dart`**：
  - 引入了 `package:flutter/services.dart` 庫，以便調用剪貼簿功能。
  - 於 `_FlippingCustomerCardState` 中實作 `_showZoomDetails(BuildContext context)` 方法。
  - 使用 `LayoutBuilder` 設計 RWD 佈局：
    - 寬螢幕下（> 500px）採用左右雙欄排列；窄螢幕（手機）下自動降級為單欄垂直滾動。
    - 大頭貼頭像、姓名/綽號、分類標籤、以及滾動的詳細備註面板皆有適當排版。
  - 在卡片正面 `_buildFront` 右上角，與背面 `_buildBack` 右上角動作列中新增放大圖示按鈕。
  ```dart
  // 詳情彈框核心 RWD 實現
  if (isWide)
    Flexible(
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          SizedBox(width: 220, child: profileSection),
          const VerticalDivider(color: Color(0xFF21262D), width: 32),
          Expanded(child: detailsSection),
        ],
      ),
    )
  else
    Flexible(
      child: SingleChildScrollView(
        child: Column(
          children: [
            profileSection,
            const SizedBox(height: 24),
            detailsSection,
          ],
        ),
      ),
    )
  ```

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **無**

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗飾**：
  - 於根目錄執行 `verify_toolpack.bat` 驗證套件與工具包對齊狀況。
  - 執行 `puro flutter analyze` 進行靜態代碼分析。
- **PM/夥伴預覽**：
  - 啟動本地伺服器後，前往 `http://localhost:8080` 的「客戶管理」分頁，點擊卡片放大圖示。
  - 可藉由調整瀏覽器寬度驗證雙欄/單欄 RWD 適配，並點選「撥打/郵件」驗證複製功能與 Toast 回報。
