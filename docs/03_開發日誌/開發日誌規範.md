# 🚀 專案開發日誌撰寫規範 (AI Assistant Developer Log Specification)

## 📌 核心宗旨
本規範旨在約束 AI 助理（Antigravity）在完成任何階段性程式碼變更、功能實作或系統架構調整後，根據使用者指令在 `docs/03_開發日誌/` 目錄下建立獨立開發日誌的行為。
日誌必須具備「極高的技術透明度」與「高可讀性」，同時滿足工程師（技術驗證）與 PM（進度追蹤）的需求。

### 📁 月份資料夾歸檔機制 (Monthly Subfolders)
所有日誌必須**按月份自動建立子資料夾**放置（例如：`docs/03_開發日誌/2026-07/`、`docs/03_開發日誌/2026-08/`），檔名維持 `YYYYMMDD_功能標題.md` 格式。這樣能確保隨時間推移日誌目錄依然極致整潔。

---

## 🛠️ 撰寫鐵律 (Core Rules)

1. **實證與代碼優先 (Code-First & Terse)**：
   - 嚴禁空泛的高層次文字描述（例如：優化了介面、提升了安全性）。
   - 必須精確指出「修改了哪些檔案」、「新增了哪些元件」、「調整了哪些關鍵邏輯/API」，並附上幾行關鍵代碼或結構變更明細。
2. **規格書與模組對齊 (Feature Alignment)**：
   - 所有功能的更新，必須嚴格對照 `docs/00_公共規格/main_spec.md` 內定義的模組名稱與編號（如：【模組一：基礎建設與安全防護】、【模組五：業務戰情中樞與排程】）。
3. **明確區分「主線」與「沙盒」 (Sandbox Isolation)**：
   - AI 自行記錄時，必須明確指出本次變更是直接作用於主專案，還是處於 `docs/01_蘿蔔_工作區/AI_對話實驗室/` 或 `docs/02_巨獸_工作區/AI_對話實驗室/` 的沙盒測試階段。
4. **環境與套件異動追蹤 (Environment Tracking)**：
   - 若變更涉及 `pubspec.yaml`、環境變數（`.env`） or 資料庫結構（`*.sql`），必須在日誌中以【⚠️ 破壞性變更/環境異動】粗體高亮標出。

---

## 📝 開發日誌標準格式 (Markdown Template)

每次新建開發日誌檔案時，必須採用以下結構填寫：

```markdown
# 📅 [YYYY-MM-DD] - 【模組名稱與編號】功能標題

> **執行狀態**：🟢 已併入主線 (src) / 🟡 沙盒實驗階段 (docs/沙盒路徑)

## 🎯 實作成果摘要
- 一句話說明本次變更為用戶或系統帶來什麼具體改變。

## 💻 技術變更明細 (Actual Technical Changes)
- **[NEW / MODIFY] `路徑/至/檔案.dart`**：
  - 具體描述修改了什麼邏輯或元件名稱。
  - 範例代碼塊（若為關鍵重構或 API 連接必填）：
    ```dart
    // 秀出最核心的 3-5 行變更程式碼
    ```
- **[DB / Schema] `supabase_schema.sql`**：
  - 記錄資料庫欄位、RLS 政策或 `policy_checkpoints` 增刪。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- 若無，請填「無」。
- **套件/Skill 異動**：新增/刪除/升級了哪些套件或 Agent Skills（同步更新 `docs/工具包.md`）。
- **密鑰異動**：`.env` 是否新增了預留變數。

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：SQL/Table 改動已配置安全性政策。
- [x] **UI/UX 規範**：符合 9 大 Agent Skills 之反俗套 (Anti-Slop) 美學與對話框防抖動標準。
- [x] **工具包同步**：已同步更新 `docs/工具包.md`。
- [x] **Null-Safety 防護**：具備 Exception 捕獲與防空值處置。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：記錄本次會話處理的核心需求背景與關鍵討點。
- **當前軟體狀態 (Current Software State)**：記錄當前分支 (`feature/lobo-...` 或 `main`) 與服務執行狀況 (`http://localhost:8080`)。
- **核心決策 (Key Architectural Decisions)**：記錄已確認的架構決策（如：保單白話庫命名、Admin 權限分工、實體名片視覺）。
- **建議接棒下一步 (Recommended Next Steps)**：為下一位開發夥伴或下一次對話提供清晰接續行動指南。
```

---

## 🛑 反面教材（嚴禁出現的寫法）
- ❌ 優化了首頁日曆的顯示效果，讓它看起來更像 iOS。 (過於空泛)
- ❌ 修復了 Windows 編譯錯誤。 (未說明是 OneDrive 與 Puro 快取相對路徑的衝突，也未說明解決手段)

## 🟢 正面教材（AI 助理必須效仿的寫法）
- 🟢 【模組五】實作 HomeScreen 橫式週曆與垂直時間軸，透過 LayoutBuilder 進行 RWD 響應式分流：寬螢幕下常駐 Sidebar 導覽，窄螢幕下自動降級為隱藏的 AppBar Drawer。
- 🟢 在 `main.dart` 的 `Supabase.initialize` 外層包覆 try-catch 區塊。若偵測到 `.env` 為預設占位字串，則捕捉異常並將 `isOfflinePreview` 狀態設為 true，於 LoginScreen 頂部渲染橘色橫幅並觸發「一鍵跳過登入」按鈕，實作平滑降級預覽。
