# 📅 2026-07-15 - 【模組一：基礎建設與安全防護 / 模組五：業務戰情中樞與排程】客戶管理板塊 UI 與雙軌 CRUD 資料庫串接落地

> **執行狀態**：🟢 已併入主線 (src) / 分支 `feature/haolu-customer-crud`

## 🎯 實作成果摘要
- 實現了高質感且支援行動端/網頁端的「客戶管理」板塊 (RWD 網格/清單)，整合了關鍵字即時搜尋篩選、新增/編輯對話框表單，並完成 Supabase 線上與記憶體離線模擬雙軌 CRUD 資料流控制。
- 修復了橫式週曆在非「今日行程」板塊下的多餘顯示，並完全解決了 Puro/Dart 編譯器在 Windows 中文路徑下的編譯 Bug。

## 💻 技術變更明細 (Actual Technical Changes)
- **[NEW] [`lib/screens/customer_management_tab.dart`](file:///C:/GitHub/project67/lib/screens/customer_management_tab.dart)**：
  - 建立響應式客戶清單 Grid/List，並透過 `SingleChildScrollView` 與 `Expanded` 配合 `TextOverflow.ellipsis` 防止 RenderFlex 在小螢幕下溢出。
  - 實作雙軌 CRUD 邏輯：
    ```dart
    // 線上與離線雙軌 CRUD 機制
    if (isOfflineMode) {
      // 離線記憶體模擬 CRUD
      setState(() => _customers.add(newCustomer));
    } else {
      // 線上 Supabase CRUD 寫入
      await Supabase.instance.client.from('customers').insert(newCustomer);
    }
    ```
- **[MODIFY] [`lib/screens/home_screen.dart`](file:///C:/GitHub/project67/lib/screens/home_screen.dart)**：
  - 整合 `CustomerManagementTab` 進入首頁主畫面工作區。
  - 優化橫式週曆顯示邏輯，僅在 `_activeMenu == '今日行程'` 時顯示。
  - 將登出按鈕 `ListTile` 包裹於 `Material` 以防止在 `ColoredBox` 內因 ink splash 導致 Flutter 框架報錯。
- **[MODIFY] [`pubspec.yaml`](file:///C:/GitHub/project67/pubspec.yaml)**：
  - 將 Dart SDK 限制調降至 `'>=3.0.0 <4.0.0'`，並調降 `flutter_lints` 至 `^3.0.0`，`supabase_flutter` 至 `^2.15.4`，以完全相容官方穩定版 Flutter 3.22 (Dart 3.4.3)。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件異動**：降級了部分 `pubspec.yaml` 相依套件版本（`supabase_flutter: ^2.15.4`、`flutter_lints: ^3.0.0`），以向下相容 Flutter 3.22。
- **專案路徑異動**：為了解決 Puro/Dart Web 編譯器在 Windows 中文路徑（`OneDrive/文件/GitHub/project67`）下無法穿透符號連結（Symlink）解析 File URI 導致 `/C:/Users/...` 報錯的 Bug，專案已成功搬移至純英文路徑 **`C:\GitHub\project67`**。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗證**：
  - 在 `C:\GitHub\project67` 執行以下指令進行自動化測試（Widget Test 均綠燈通過）：
    ```powershell
    flutter test
    ```
- **PM/夥伴預覽**：
  - 請在專案根目錄下，切換為命令提示字元 (cmd) 執行以下指令啟動 Web Server：
    ```cmd
    C:\src\flutter\bin\flutter run -d web-server
    ```
  - 複製顯示的網址（如 `http://localhost:55055`）在 Chrome 中開啟，點擊頂部黃色橫幅即可跳過登入進入離線預覽，在左側點選「客戶管理」即可進行卡片搜尋、新增客戶表單等完整操作！
