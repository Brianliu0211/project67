# 📅 [2026-09-01] - 【模組四：排程與提醒推播】Google 日曆雙向自動同步與專屬次日曆實作

> **執行狀態**：🟢 已併入特徵分支 (feature/beast-google-calendar-sync)

## 🎯 實作成果摘要
- 完成 App 拜訪行程 (`schedule_events`) 與 Google Calendar API v3 之雙向自動同步服務，自動維護專屬次日曆「保險助手 (Insurance Helper)」，實作 `extendedProperties.private` ID 比對、LWW 時間戳衝突解決與離線降級防護，全站單元測試 31/31 綠燈通過。

## 💻 技術變更明細 (Actual Technical Changes)

- **[NEW] `lib/services/google_calendar_sync_service.dart`**：
  - 封裝單例服務 `GoogleCalendarSyncService.instance`。
  - 實作次日曆自動搜尋與建立 `getOrCreateSecondaryCalendar`，預設名稱為 `保險助手 (Insurance Helper)`。
  - 實作 `pushLocalToRemote` 與 `pullAndAlignEvents` 雙向同步邏輯：
    ```dart
    final googleEvent = calendar.Event()
      ..summary = event.title
      ..description = event.description
      ..location = event.location
      ..start = calendar.EventDateTime(dateTime: event.startAt.toUtc())
      ..end = calendar.EventDateTime(dateTime: event.endAt.toUtc())
      ..extendedProperties = calendar.EventExtendedProperties(
        private: {'insurance_helper_id': event.id},
      );
    ```
- **[MODIFY] `lib/services/schedule_service.dart`**：
  - 於 `createEvent`、`updateEvent`、`deleteEvent` 及 `fetchEvents` 中非同步掛載 `_syncToGoogleCalendar` 鉤子與 `pullAndAlignEvents`。
- **[MODIFY] `lib/screens/settings_screen.dart`**：
  - 於第三方服務連線卡片中新增「Google 日曆行程雙向自動同步」Switch 開關，並連動 `SharedPreferences` 持久化。
- **[MODIFY] `lib/widgets/schedule_event_dialog.dart`**：
  - 於行程彈窗標題列新增「Google 已對齊 / 自動對齊 Google」狀態徽章。
- **[DB / Schema] `supabase_schema.sql` & `supabase/migrations/20260901_google_calendar_sync_schema.sql`**：
  - 於 `public.schedule_events` 表新增 `sync_status TEXT DEFAULT 'local_only'` 欄位與對應 Migration 腳本。
- **[NEW] `docs/02_巨獸_工作區/想法存放區/Google_Cloud_Console申請與連線交接指南.md`**：
  - 撰寫給蘿蔔 (lobo) 的 Google Cloud Console 憑證申請與功能連線 SOP。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件/Skill 異動**：引入 `googleapis (^13.2.0)`、`google_sign_in (^6.2.1)` 與 `extension_google_sign_in_as_googleapis_auth (^2.0.12)`；已同步更新至 `docs/工具包.md`。
- **密鑰異動**：預留 `GOOGLE_CLIENT_ID` 環境變數。

## 🚦 Quality Gate 品質門檻稽核
- [x] **Supabase RLS 安全**：`schedule_events` 新欄位 `sync_status` 繼承現有用戶隔離 RLS 政策。
- [x] **UI/UX 規範**：對齊 Design System HSL/青藍色調與深淺色主題規範，通知採用 `CustomToast`。
- [x] **工具包同步**：已同步更新 `docs/工具包.md`。
- [x] **Null-Safety 防護**：具備 Exception 捕捉與離線 `pending_push` 平滑降級機制。

## 🤝 [Handoff 狀態條言] (Session Cross-Handover State)
- **會話脈絡 (Session Context)**：本次會話由開發者巨獸 (beast) 開工，完成模組四「Google 日曆雙向自動同步」功能開發。
- **當前軟體狀態 (Current Software State)**：位於 `feature/beast-google-calendar-sync` 特徵分支。`flutter build web --release` 編譯成功，本地 Web 伺服器運作於 `http://localhost:8080`，31/31 全站單元測試綠燈通過。
- **核心決策 (Key Architectural Decisions)**：
  1. 採用專屬次日曆「保險助手 (Insurance Helper)」，防範干擾業務員預設個人主日曆。
  2. 採用 `extendedProperties.private['insurance_helper_id']` 進行唯一 ID 比對。
  3. 無 API 金鑰或網路時自動平滑降級，絕不上報崩潰。
- **建議接棒下一步 (Recommended Next Steps)**：
  1. 蘿蔔可依照 `docs/02_巨獸_工作區/想法存放區/Google_Cloud_Console申請與連線交接指南.md` 申請 Google Cloud Client ID 並填入 Supabase Dashboard。
  2. 提交本次 PR 並 Merge 回 `main` 主線。
