# 📅 2026-07-17 - 【環境與部署】Phase 0.95 部署自動化 (CI/CD) 與 build 目列 Git 追蹤清理

> **執行狀態**：🟢 已併入主線 (src) / 分支 `feature/beast-cicd`

## 🎯 實作成果摘要
- 建立 GitHub Actions 工作流，實現推送到 `main` 分支時，雲端自動編譯 Flutter Web 並發布至 Vercel，提供「治本」的部署方式。
- 修改 `.gitignore` 並自 Git 移除 `build/` 目錄的追蹤，大幅減肥專案倉庫體積，避免本地 Web 編譯檔案的提交衝突。
- 撰寫 Vercel 金鑰配置與給蘿蔔的交接文件。

## 💻 技術變更明細 (Actual Technical Changes)
- **[NEW] [`.github/workflows/deploy.yml`](file:///C:/GitHub/project67/.github/workflows/deploy.yml)**：
  - 建立 GitHub Actions 部署工作流，使用 Flutter SDK `3.22.2` 與 Java 17 環境進行 Web Release 編譯，並整合 Vercel CLI 進行 Prebuilt 部署。
- **[MODIFY] [`.gitignore`](file:///C:/GitHub/project67/.gitignore)**：
  - 移除 `!/build/web/` 白名單，將整個 `build/` 目錄納入 Git 忽略範圍。
- **[DELETE Git Tracking] 移除 `build/` 追蹤**：
  - 執行 `git rm -r --cached build/` 移除 30+ 個 Web 靜態發布檔案（包含大體積的 CanvasKit.wasm、main.dart.js 等）的 Git 追蹤。
- **[NEW] [`docs/02_巨獸_工作區/想法存放區/CI_CD部署設定與給蘿蔔的交接說明.md`](file:///C:/GitHub/project67/docs/02_巨獸_工作區/想法存放區/CI_CD部署設定與給蘿蔔的交接說明.md)**：
  - 建立專屬交接說明，包含 Vercel 密鑰（Token, Org ID, Project ID）的詳細取得路徑。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件異動**：無。
- **金鑰異動**：GitHub Actions 需要配置三筆 Repository Secrets：`VERCEL_TOKEN`、`VERCEL_ORG_ID`、`VERCEL_PROJECT_ID`。
- **部署方式變更**：此後**不再需要**在本地執行 `flutter build web` 並提交 `build/web`！合併到 `main` 後，Vercel 部署將 100% 由 GitHub Actions 自動化託管。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗證**：
  - 檢查 Git 狀態，確認 `build/` 資料夾已被忽略且不被 Git 追蹤。
- **PM/夥伴（蘿蔔）預覽與對接**：
  - 請蘿蔔在 GitHub Repository Settings 中將 `VERCEL_TOKEN`、`VERCEL_ORG_ID` 和 `VERCEL_PROJECT_ID` 填入 Secrets。
  - 將本分支合併至 `main` 後，觀察 GitHub Actions 頁面是否成功跑完綠燈，且 Vercel 網址已同步更新。
