# 📅 2026-07-17 - 【模組一：客戶基本資料庫與 UI 刻劃】修復 Supabase 線上資料表欄位對齊

> **執行狀態**：🟢 已併入主線 (src)

## 🎯 實作成果摘要
- 解決線上 Supabase `customers` 資料表缺少 `nickname` 與 `avatar_url` 欄位，導致前端在線上模式下新增客戶失敗並報錯 `PGRST204` 的問題。

## 💻 技術變更明細 (Actual Technical Changes)
- **[DB] `supabase_schema.sql` / Supabase 線上變更**：
  - 於 Supabase 線上專案 `algufuoxkeizxwkofmmp` 執行 DDL 欄位追加，使其實體與本地定義檔對齊：
    ```sql
    ALTER TABLE public.customers ADD COLUMN nickname TEXT;
    ALTER TABLE public.customers ADD COLUMN avatar_url TEXT;
    ```
- **[BRANCH] 建立開發分支**：
  - 建立並切換至 `feature/lobo-fix-customer-db-schema`，比對驗證所有客戶管理相關 CRUD 的 API 欄位對齊情況。

## ⚠️ 環境異動與破壞性變更 (Environment & Breaking Changes)
- **套件異動**：無。
- **密鑰異動**：無。
- **⚠️ 資料庫異動**：線上資料庫 `public.customers` 資料表補齊 `nickname` 與 `avatar_url` 欄位。

## 🚦 團隊協作與驗證指南 (Verification Plan)
- **工程師驗證**：
  - 在本機或 Vercel 部署版，使用線上資料庫模式登入，測試「新增客戶」與「編輯客戶」表單。
  - 確保填寫「客戶綽號」與「照片網址」後能成功寫入線上資料庫，無 `PGRST204` 或任何 PostgREST 報錯，並順利觸發綠色成功 Toast 通知。
- **PM/夥伴預覽**：
  - 開啟網頁直接操作新增客戶功能，確認流程流暢，且卡片正面可正常顯示照片頭像與 `姓名 (綽號)`。
